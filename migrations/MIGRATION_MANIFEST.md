# Migration manifest & replay authority

> **NON-AUTHORITATIVE AS OF S4 (2026-08-15).** As of `MESA_REMEDIATION_PLAN_FINAL_V2_1_2_2026-08-15.md`
> slice S4, this file is a human-readable **narrative** only. The authoritative record of what
> was applied, its checksum, and whether it is **verified** or **bootstrapped_unverified** is
> `public.ladieci_schema_migrations` (§15) — a project-owned, service_role-only, append-only
> ledger. This manifest is never re-read as compatibility authority by any runtime code; it
> stays useful as inventory and for the git-history/commit-authorship trail it has always
> carried, but if it and `ladieci_schema_migrations` ever disagree, the ledger wins. Rows below
> stop growing as the primary record from row 74 onward (the first three `ladieci_schema_
> migrations`-owning migrations themselves) — new migrations register directly in the ledger;
> this file may still gain a row per migration for human continuity, but that row is decorative.
>
> This file's own claim, one paragraph below, that "this project has no Supabase CLI migration
> ledger" was already **stale before S4** — `supabase_migrations.schema_migrations` exists live
> (67 rows as of S4, written by Supabase MCP `apply_migration`) — see S0 §K / S4's own header
> comments for the full detail. That table remains Supabase's own, read-only to this project,
> and is distinct from both this manifest and from `ladieci_schema_migrations`.

**Scope of this file.** This project has **no Supabase CLI migration ledger** (there is no
`supabase/` directory and no `supabase_migrations.schema_migrations` table populated by the
CLI). Migrations are plain `migrations/YYYY-MM-DD_<name>.sql` files, applied **manually**
against staging and tracked by two things only: **git history** (the commit that introduced
each file is the authoritative "created / applied" record) and **static text tests**
(`tests/*Migration.test.js`).

Because ordering is otherwise inferred from filenames, this manifest is the **explicit replay
authority**: a replay MUST apply forward migrations in ascending `apply_order` below — **not**
in lexical filename order — and MAY use the recorded `sha256` to detect an already-applied
migration. Introduced during **S2-7C1** (reference date **2026-07-23**) to reconcile the two
future-dated S2-7B/S2-7C migrations without renaming or re-running any SQL.

---

## S2-7C1 reconciliation — the two account/workspace migrations

| field | S2-7B workspace foundation | S2-7C account boundary |
|---|---|---|
| source filename | `2026-07-24_workspace_foundation.sql` | `2026-07-24_account_auth_boundary.sql` |
| rollback filename | `2026-07-24_workspace_foundation.ROLLBACK.sql` | `2026-07-24_account_auth_boundary.ROLLBACK.sql` |
| version prefix (filename) | `2026-07-24` | `2026-07-24` |
| introduced by commit | `140c8cd` | `0222f04` |
| commit author date | **2026-07-23 14:55:08 +0200** | **2026-07-23 15:20:37 +0200** |
| forward sha256 | `42c949a39ce7d34dc78f904bc898c8537572f2edc71eebf8d1c4fce06bc4a436` | `c46be5b362fa6541de345a95ffca639260b413e90230ab5b877f2afd6ed80e3a` |
| rollback sha256 | `eff05f9a81449b54c6c9788db81dd3a47af840f1faeb16ed54c6b4c552a48ca5` | `10d25240b8773b530b7059871dcc4e46832db80802ef9b40254b80be80bb1812` |
| **apply_order** | **29** | **30** |

> The reference date at reconciliation time is **2026-07-23**. Both filenames carry the
> version prefix **2026-07-24**, i.e. **one day in the future** relative to both their own
> commit date and the reference date.

### Two hazards identified

1. **Future-dated prefix (cosmetic).** `2026-07-24` sorts after every `≤ 2026-07-23`
   migration, so relative to the *current* set the two files still land last, in the right
   place. The wrong date does not, by itself, skip/duplicate/reorder anything.

2. **Intra-prefix lexical inversion of the B→C dependency (real).** Both files share the
   `2026-07-24` prefix, and lexically `account_auth_boundary` **precedes** `workspace_foundation`.
   That is the **reverse** of the true dependency: `2026-07-24_account_auth_boundary.sql`
   (S2-7C) runs `REVOKE UPDATE ON public.user_profiles`, `CREATE TRIGGER … ON
   public.user_profiles`, and triggers on `public.workspace_account_audit` — objects
   **created by** `2026-07-24_workspace_foundation.sql` (S2-7B). A replay driver that sorts
   by filename would apply S2-7C **before** S2-7B and fail (`public.user_profiles` does not
   exist yet). The commit order (`140c8cd` before `0222f04`) is the correct order and is the
   **opposite** of the lexical order.

### Adopted reconciliation

**Keep the filenames as-is (no rename); pin the true order in this manifest.**

- **No SQL is re-run**, **no history is wiped**, **no file is blind-renamed** — as required.
- Renaming the files to a `2026-07-23` prefix was rejected: it would (a) require rewriting the
  applied commits `140c8cd` / `0222f04` (forbidden), and (b) **diverge the source filename
  from the name under which the SQL was applied to staging**, which is itself the skip/dup
  hazard we are avoiding. Keeping the applied name guarantees *source identity == applied
  identity*.
- The dangerous signal is not the date but the lexical order; this manifest overrides it by
  making `apply_order` (which follows commit/dependency order: **29 = workspace_foundation**,
  then **30 = account_auth_boundary**) the authority, independent of the misleading filenames.
- A short guard test (`tests/migrationManifestOrder.test.js`) asserts the manifest keeps
  S2-7B strictly before S2-7C and that S2-7C references objects S2-7B creates — so the
  inversion can never be silently reintroduced.

### Replay-safety proof

A future replay driven by this manifest **cannot**:

- **skip** — every forward migration in `migrations/*.sql` has exactly one `apply_order` row
  (full table below); nothing is unlisted.
- **duplicate** — each row carries the exact source `sha256`; a driver applies a row only if
  its checksum is not already recorded as applied, so a re-run is a no-op rather than a second
  `CREATE`.
- **reorder** — the driver follows ascending `apply_order`, which encodes commit/dependency
  order. In particular `workspace_foundation` (29) is guaranteed to run before
  `account_auth_boundary` (30), regardless of the fact that the filenames sort the other way.

---

## 2026-08-09 staging reality reconciliation

Rows 44, 50, 51, 53, 54 and 55 below previously carried `DRAFT — NOT APPLIED` markers. Those
markers had **drifted from the real staging database state** — the migrations were, in fact,
already applied to `tdikhfeinufaahagmpjz` (LaDieci10App-staging), most likely by the owner or
tooling outside the sessions that authored this manifest's inline comments. This was discovered
and confirmed on 2026-08-09 during a staging-safety-freeze session, via **read-only** evidence
only (Supabase PostgREST OpenAPI schema introspection and `select` queries, `service_role` key
injected through `railway run --service fearless-reverence`; zero writes, zero RPC calls):

- `service_closeout_attempts`, `service_closeout_snapshots`, `service_incidents`,
  `service_incident_resolutions` and `archived_order_financial_resolutions` all exist and are
  queryable (rows 53–55).
- `table_sessions` and `auth_assign_table_session_waiter_v3` exist (row 44).
- Every `messa_*` RPC is gone and every `mesa_*` RPC exists (rows 50–51's cutover is complete
  on the DB side). The currently-deployed staging backend at the time (commit `9569023f8`,
  2026-07-30) predates all Mesa code and calls neither name, so this created no live outage.

Each affected row below has been updated in place with its specific verification evidence.
**No SQL was run, no data was changed, and no row was marked applied "as of today"** — the
correction only reflects when reality already diverged from the manifest text.

**Row 56 remains genuinely unapplied** — confirmed by the same method (its one new,
uniquely-identifying RPC, `mesa_release_empty_session_auto_v1`, is absent from the live
schema) — and is marked `UNAPPLIED — V2 legacy-policy migration; do not deploy pending V3
replacement` so a future replay/agent does not mistake its presence in this directory for
approved, ready-to-apply work.

---

## Full ordered manifest (all forward migrations)

`apply_order` follows commit-introduction order. Where a filename's version prefix disagrees
with its commit date (rows 8, 11, 29, 30) the commit date governs.

| # | version prefix | filename | introduced-by | commit date | sha256 (16) |
|--:|---|---|---|---|---|
| 1 | 2026-05-14 | 2026-05-14_geo_cache_confidence.sql | fab25bc | 2026-05-14 | 598f1f38caffe541 |
| 2 | 2026-05-25 | 2026-05-25_manual_giros.sql | cfba8b9 | 2026-05-25 | 46c748f218f411cb |
| 3 | 2026-05-29 | 2026-05-29_manual_giros_entrega_ref.sql | 3061989 | 2026-05-29 | 352741f5ad731f19 |
| 4 | 2026-05-29 | 2026-05-29_manual_giros_hora_ref.sql | 9edcdb9 | 2026-05-29 | 444f31d91d9b14ac |
| 5 | 2026-06-02 | 2026-06-02_driver_schedule_fields.sql | 5f7f31d | 2026-06-02 | 36a1447f9e69cc51 |
| 6 | 2026-06-05 | 2026-06-05_order_state_transition_logs.sql | 5d9e2de | 2026-06-05 | 6a80a260b9a7e77c |
| 7 | 2026-07-10 | 2026-07-10_p0_lock_sensitive_reads.sql | 3e0814c | 2026-07-10 | 69fdb47bda938354 |
| 8 | 2026-07-13 | 2026-07-13_auth_active_events.sql | 3888d22 | 2026-07-14 | a2ca58a671383c12 |
| 9 | 2026-07-13 | 2026-07-13_auth_rpc.sql | eaad05b | 2026-07-13 | 7e9a6ce04f5b7290 |
| 10 | 2026-07-13 | 2026-07-13_auth_v2_foundation.sql | 1fef81d | 2026-07-13 | 9af802a1c54ae7c1 |
| 11 | 2026-07-14 | 2026-07-14_auth_recovery_windows.sql | 8a1a4ff | 2026-07-15 | 61b7fece50873718 |
| 12 | 2026-07-15 | 2026-07-15_auth_admin_access_management.sql | 785d317 | 2026-07-15 | c18838668c3d3219 |
| 13 | 2026-07-15 | 2026-07-15_auth_unlocked_event.sql | 15512c9 | 2026-07-15 | 4728a8de17371a94 |
| 14 | 2026-07-15 | 2026-07-15_b7_financial_ledger_foundation.sql | 93861d8 | 2026-07-15 | f9f51b24f32c331e |
| 15 | 2026-07-15 | 2026-07-15_b7_financial_ledger_grant_hardening.sql | 2955c27 | 2026-07-15 | a783b8d57b01abde |
| 16 | 2026-07-15 | 2026-07-15_b7_payment_basis_rpcs.sql | 700c03b | 2026-07-15 | 53f572f8fa61a557 |
| 17 | 2026-07-15 | 2026-07-15_b7_refund_void_rpcs.sql | e72218a | 2026-07-15 | bebb7de415c20d23 |
| 18 | 2026-07-16 | 2026-07-16_b7_void_digest_replay_fix.sql | aca5969 | 2026-07-16 | 4021e40294324feb |
| 19 | 2026-07-17 | 2026-07-17_b7_financial_session_version_guard.sql | a5ef761 | 2026-07-17 | f3d56d58b834ce72 |
| 20 | 2026-07-19 | 2026-07-19_b7_payment_basis_historical_replay_fix.sql | 2ec5246 | 2026-07-19 | 8707762a27ab5f99 |
| 21 | 2026-07-20 | 2026-07-20_config_write_revoke.sql | 573398b | 2026-07-20 | b46a6bdeac02022c |
| 22 | 2026-07-20 | 2026-07-20_manual_giros_rls_lockdown.sql | 573398b | 2026-07-20 | 1cf173b97249f40b |
| 23 | 2026-07-20 | 2026-07-20_rider_trip_rpcs.sql | 77693e9 | 2026-07-20 | 450c5d75edf210b8 |
| 24 | 2026-07-21 | 2026-07-21_fix_rider_delivery_log_timestamps.sql | 9fef8d3 | 2026-07-21 | bec2490d9d32e56e |
| 25 | 2026-07-21 | 2026-07-21_fix_rider_trip_json_null_idempotency.sql | 821d56b | 2026-07-21 | 73d4b2fa60cc6925 |
| 26 | 2026-07-22 | 2026-07-22_service_session_identity.sql | 207672b | 2026-07-22 | 835d9d6d0fa7d971 |
| 27 | 2026-07-23 | 2026-07-23_storico_drop_legacy_orden_fecha_uniqueness.sql | 0cf9320 | 2026-07-23 | 993ab0c39e842814 |
| 28 | 2026-07-23 | 2026-07-23_storico_session_order_uq_nonpartial.sql | 4ecf3e1 | 2026-07-23 | a074088c269073e0 |
| 29 | 2026-07-24 → **B (S2-7B)** | 2026-07-24_workspace_foundation.sql | 140c8cd | 2026-07-23 | 42c949a39ce7d34d |
| 30 | 2026-07-24 → **C (S2-7C)** | 2026-07-24_account_auth_boundary.sql | 0222f04 | 2026-07-23 | c46be5b362fa6541 |
| 31 | 2026-07-24 → **S2-7D** | 2026-07-24_workspace_owner_pin.sql | bd6f363 | 2026-07-24 | e5c02d93520ee665 |  <!-- APPLIED on staging -->
| 32 | 2026-07-25 → **S2-7D2 A** | 2026-07-25_canonical_pin_rotation.sql | pending | 2026-07-25 | bfa0711f14dde6be |  <!-- DRAFT — apply BEFORE the backend cutover -->
| 33 | 2026-07-26 → **S2-7D2 B** | 2026-07-26_disable_legacy_pin_rotation.sql | pending | 2026-07-26 | 510ab39362dd4ff7 |  <!-- DRAFT — apply ONLY AFTER the cutover is deployed+verified -->
| 34 | 2026-07-26 → **S2-7D6B** | 2026-07-26_two_service_identity.sql | 9a86fb1 | 2026-07-26 | 3027d7291d3c6c6d |  <!-- APPLIED on staging -->
| 35 | 2026-07-27 → **S2-7D6E** | 2026-07-27_order_state_logs_session_identity.sql | pending | 2026-07-27 | f1448c0f33ef8bad |  <!-- DRAFT — NOT APPLIED; purely additive (nullable column + partial indexes + soft trigger), independent of the payment wiring -->
| 36 | 2026-07-27 → **S2-7D6E3 A** | 2026-07-27_s2_7d6e3a_rider_ledger_writer_additive.sql | pending | 2026-07-27 | 2c0796cc34aaa546 |  <!-- DRAFT — NOT APPLIED. Supersedes the single-file S2-7D6E2 draft (split after an audit found it coupled DB and backend deploys). Purely additive: creates _ledger_write_payment (session-scoped idempotency — fixes a gap present since 2026-07-19 that the 2026-07-26 session-scoped indexes never got matched in code), replaces order_mark_paid to delegate to it (admin/operator authority byte-unchanged), adds rider_collect_and_complete_stop. Does NOT drop complete_rider_stop — an older backend keeps working unmodified. Apply independently of the backend deploy. -->
| 37 | 2026-07-27 → **S2-7D6E3 D** | 2026-07-27_s2_7d6e3d_retire_complete_rider_stop.sql | pending | 2026-07-27 | 5766f9eebe9c1cee |  <!-- DRAFT — NOT APPLIED. Cleanup-only: DROPS the ledger-less complete_rider_stop(text,boolean,text). Apply ONLY AFTER row 36 is applied AND the backend commit repointing riderTrip.completeStop to rider_collect_and_complete_stop has deployed AND a live smoke test has passed. Guard refuses to run unless rider_collect_and_complete_stop already exists. -->
| 38 | 2026-07-28 → **SERVICE ORDER NUMBER** | 2026-07-28_service_order_number.sql | 846a58f | 2026-07-28 | 18e91cd253cbad04 |  <!-- APPLIED on staging; trigger wiring re-applied explicitly and verified after the forward was made self-contained. -->
| 39 | 2026-07-29 → **V3-A** | 2026-07-29_v3a_access_control_foundation.sql | d3610ae | 2026-07-29 | 4d3aae1ea98ba87c |  <!-- APPLIED on staging 2026-07-29 (tdikhfeinufaahagmpjz). Additive/foundation only: widens the auth_actors.role CHECK to a transitional union (legacy admin/operator/rider values UNCHANGED on every row — real per-row conversion deferred to V3-C), adds display_name/created_at/created_by, adds auth_actor_pin_fingerprints + access_management_idempotency (both inert, 0 rows), widens auth_audit_event_chk. Does not lift the actor-id CHECK (V3-D) and does not touch table_sessions (V3-G). Idempotent-re-apply fix landed in commit d480f03 before staging apply. -->
| 40 | 2026-07-29 → **V3-B** | 2026-07-29_v3b_auth_set_actor_pin_v3.sql | f75a8f4 | 2026-07-29 | f2e887c6dce6abe4 |  <!-- APPLIED on staging 2026-07-29 (tdikhfeinufaahagmpjz), ledger version 20260729151034. Additive only: creates auth_set_actor_pin_v3 ALONGSIDE the still-untouched, still-sole-live auth_set_actor_pin_v2 (verified byte-identical pre/post apply). Adds atomic dual-key fingerprint writes and removes v2's hardcoded 3-role closed-list pre-check (the authoritative row-based role check is unchanged). No Node code calls v3 yet — still completely unwired. -->
| 41 | 2026-07-29 → **V3-C** | 2026-07-29_v3c_auth_change_actor_role.sql | 35344f3 | 2026-07-29 | 0825d580cc96eb10 |  <!-- APPLIED on staging 2026-07-29 (tdikhfeinufaahagmpjz), ledger version 20260729162816. Relaxes the hardcoded 1:1 auth_actors_actor_role_map CHECK to permit any non-owner actor to hold any role except admin/owner (owner stays pinned to admin; no row's role value changed on apply — verified byte-identical pre/post). Creates auth_change_actor_role_v3: a dormant, owner-authorized, session-bound-idempotent RPC converting ONE non-owner actor to ONE of the 5 assignable V3 roles (cashier/waiter/kitchen/rider/shift_manager), auditing role_changed + session_invalidated on every real change. No Node code calls it yet — still completely unwired. -->
| 42 | 2026-07-29 → **V3-D** | 2026-07-29_v3d_dynamic_access_user.sql | d8cfc96 | 2026-07-30 | c64249995bbf4fbd |  <!-- APPLIED on staging 2026-07-30 (tdikhfeinufaahagmpjz), ledger version 20260730085937. Lifts the fixed four-actor identity ceiling: widens auth_actors_actor_chk/created_by_chk/updated_by_chk and auth_audit_target_actor_chk/by_actor_chk to also accept a canonical UUID text form (strict superset — no existing row changed on apply, verified byte-identical pre/post). Adds auth_actors_display_name_chk (bound 120, trimmed, control-char-free). Creates auth_create_access_user_v3 (server-generates the UUID, frozen safe defaults, pin_hash NULL, no fingerprint) and auth_rename_access_user_v3 (display_name only, self-rename allowed), both session-bound-idempotent and auditing user_created/user_renamed. No Node code calls either yet — still completely unwired. -->
| 43 | 2026-07-30 → **V3-E** | 2026-07-30_v3e_access_user_lifecycle.sql | pending | 2026-07-30 | cb570e3d738f2aeb |  <!-- DRAFT — NOT APPLIED. Adds no new/widened constraint (every audit event it uses — user_deactivated/user_reactivated/credential_cleared/session_invalidated — was already reserved by V3-A). Creates auth_set_access_user_active_v3: one canonical active-state writer for both directions, distinguished by a boolean + by distinct stable idempotency action ids (deactivate_access_user_v3/reactivate_access_user_v3); bumps session_version on BOTH directions (deliberately stricter than the legacy auth_set_active, which only bumps on disable). Creates auth_clear_access_user_credential_v3: clears pin_hash, deletes every fingerprint row for the target, resets failed_count/locked_until, bumps session_version once — releasing the PIN for reuse, distinct from deactivation (which preserves the credential and its reservation). Both owner-authorized by role, workspace-scoped, session-bound-idempotent. Target eligibility is a POSITIVE allowlist (operator/legacy_operator/cashier/waiter/kitchen/rider/shift_manager), not a denylist — corrected in a pre-staging security-closure pass, checksum updated accordingly. Idempotency replay is evaluated only AFTER acting-owner and target-eligibility are re-proved under lock, so a revoked/demoted/deactivated acting owner cannot replay a stored success. No Node code calls either RPC yet — still completely unwired. -->
| 44 | 2026-07-31 → **V3-G** | 2026-07-31_v3g_waiter_table_assignment_safety.sql | pending | 2026-07-31 | 4956087056f1a299 |  <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) — reconciled 2026-08-09 (see "2026-08-09 staging reality reconciliation" below); this row previously read DRAFT/NOT APPLIED, which had drifted from the live database. Verified read-only via PostgREST OpenAPI schema (service_role key, GET only): `table_sessions` table and `auth_assign_table_session_waiter_v3` RPC both present on staging. PATH B: no table/mesa/dine-in concept existed anywhere in this codebase (confirmed by audit); creates the minimal canonical table_sessions (workspace-scoped, nullable assigned_waiter_actor — an open session MAY be legitimately unassigned) + append-only table_session_assignment_history. Creates auth_assign_table_session_waiter_v3: dormant, owner-authorized, session-bound-idempotent, frozen actors-before-sessions lock order (workspace → every involved actor in ONE deterministic ORDER BY actor statement, including the acting owner, the session's current assignee, and the requested waiter → the target session, ORDER BY id → idempotency only after all validation). Revises auth_set_access_user_active_v3 (CREATE OR REPLACE, guarded by an exact-signature predecessor check proving V3-E's own function is what is being replaced) to add exactly one rule: a role='waiter' target with one or more OPEN assigned table sessions cannot be deactivated — AUTH_WAITER_HAS_OPEN_TABLES, raised after authorization/idempotency-replay evaluation but before any actor mutation, audit insertion, or idempotency insertion. Every other V3-E behavior is preserved byte-for-byte; reactivation and non-waiter deactivation are unaffected. V3-G.1 (2026-07-31, checksum updated) additionally revises auth_change_actor_role_v3 (CREATE OR REPLACE, guarded by an exact-signature predecessor check proving V3-C's own function is what is being replaced) with the SAME rule for role changes away from waiter, and CORRECTS a source-audit finding: the committed V3-C body evaluated idempotency replay BEFORE actor locking/authorization (opposite of V3-E's already-corrected order) — this revision reorders it to match V3-E's pattern (actor lock/authorize → idempotency replay short-circuit → expected-role snapshot check → waiter guard → mutate), so neither a revoked/demoted acting owner NOR a stale idempotency record can bypass current authorization or the new guard. Does not widen auth_audit_event_chk (the dedicated history table is the source of truth). V3-G.2 (2026-07-31, checksum updated again) fixes a live historical-replay bypass found by reproducing the V3-G.1 body against real PostgreSQL: since the idempotency short-circuit still ran BEFORE the waiter guard, a stored role-change success could be replayed after an intervening state change (target reassigned back to waiter, given an open table) and still return the old success. Fixed by splitting idempotency into an immediate changed-payload conflict check (unweakened) and a deferred stored-response return that now happens AFTER the guard is re-evaluated against current state on every call, replay or not. No Node code calls any of these RPCs yet — still completely unwired. -->
| 45 | 2026-08-01 → **V3-H MESA** | 2026-08-01_v3h_messa_billing_foundation.sql | aeab2ff | 2026-08-01 | 9ed5d790049bc270 |  <!-- APPLIED on staging 2026-08-01 (tdikhfeinufaahagmpjz), ledger version 20260801181607. Adds the five-table responsive floor, one independent kitchen command per addition, immutable per-unit settlement lines, append-only mixed-method payment transactions and allocations, partial pay states in the canonical financial ledger, exact Roman-cent division, product-selection and free-amount payments, and guarded owner layout editing. A Mesa has only two operational states: free or open account. Full payment permanently closes that account ID and makes the Mesa free immediately; a later order opens a brand-new account/session ID with independent commands, products, covers and payments. No paid-but-occupied state or manual release workflow exists. Runtime HTTP remains fail-closed behind MESSA_HTTP_ENABLED=true. -->
| 46 | 2026-08-01 → **V3-H.1 MESA INDEXES** | 2026-08-01_v3h1_messa_fk_indexes.sql | 1505250 | 2026-08-01 | d16fc57267c0bd98 |  <!-- APPLIED on staging 2026-08-01 (tdikhfeinufaahagmpjz), ledger version 20260801181929. Adds six covering indexes requested by the Supabase foreign-key advisor; no rows, functions, grants, RLS policies or runtime behavior change. -->
| 47 | 2026-08-01 → **V3-H.1A MESA PAYMENT DIGEST PATH** | 2026-08-01_v3h1a_messa_payment_digest_path.sql | 58dd10c | 2026-08-01 | 97a4e11977d43313 |  <!-- APPLIED on staging 2026-08-01 (tdikhfeinufaahagmpjz). Changes only messa_post_payment_v1's pinned search_path to include Supabase's extensions schema, where pgcrypto.digest lives. No rows or function body changed. Verified with a real two-command Mesa checkout: one €13.50 payment, two allocations, two financial events, exact idempotent replay and immediate table release. -->
| 48 | 2026-08-01 → **V3-H.2 MESA RESERVATIONS** | 2026-08-01_v3h2_messa_reservations.sql | e3e90ad | 2026-08-01 | b2d165b1d97f9c8c |  <!-- APPLIED on staging as Supabase ledger version 20260802113013 (`v3h2_messa_reservations_20260802`). Adds two-hour Mesa reservations with customer name, phone, covers and note; today bookings project as a red floor warning while the underlying table account remains independently free/open. Operator, cashier, waiter, shift-manager and owner/admin roles may create, edit, move, cancel and mark no-show; seating atomically opens a fresh table account with the reserved covers. Optimistic versions prevent lost updates, a workspace write lock prevents overlapping bookings, full payment completes a seated reservation, and future bookings prevent soft-removing the physical table. RLS is enabled and anon/authenticated access is revoked; all mutation RPCs are SECURITY INVOKER + service_role-only. -->
| 49 | 2026-08-02 → **SERVICE CLOSE LIVE-WORK GUARD** | 2026-08-02_service_close_live_work_guard.sql | a43dbb5 | 2026-08-02 | c036a2ceab7414bc |  <!-- APPLIED on staging as Supabase ledger version 20260802101427 (`service_close_live_work_guard_20260802`). Adds a last-line BEFORE UPDATE guard on service_sessions: a transition to closed is refused while the same service still owns an open Mesa account or any non-terminal/unknown live order. Existing historical rows were not rewritten. Live transaction/rollback probes proved both order and Mesa refusal paths without leaving test residue. -->
| 50 | 2026-08-02 → **V3-I MESA COVERS DEFERRED** | 2026-08-02_v3i_mesa_covers_deferred.sql | pending | 2026-08-02 | bdd5807446fbce55 |  <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) — reconciled 2026-08-09 (see "2026-08-09 staging reality reconciliation" below); this row previously read DRAFT/NOT APPLIED, which had drifted from the live database. Verified read-only via PostgREST OpenAPI schema (service_role key, GET only): `mesa_release_empty_session_v1` RPC present on staging (this migration's own function, later renamed by row 51 — see that row). Defers a walk-in Mesa's covers_total from open time to its first comanda: DROPs the NOT NULL, adds a monotonic guard trigger (once set, covers can only stay the same or grow), and extends messa_prepare_table_order_v1 to atomically require+lock in real covers on the first comanda via a new ephemeral ordenes.table_covers_total_input column (always nulled back out before the row is written) -- no second endpoint, no window where covers register but the comanda can still fail. messa_post_payment_v1 refuses with MESSA_COVERS_NOT_SET while covers are still NULL. messa_open_session_v1 keeps its exact 5-argument signature (covers now optional/nullable) and maps a true concurrent double-open's unique_violation to a clean MESSA_TABLE_ACCOUNT_OPEN. New messa_release_empty_session_v1 lets an accidentally-opened table (covers_total IS NULL, which by construction means zero comande) be released with no payment. messa_open_reservation_v1 is untouched -- a reservation's covers are real at seating time already. -->
| 51 | 2026-08-02 → **V3-J MESA NOMENCLATURE CUTOVER** | 2026-08-02_v3j_mesa_nomenclature_cutover.sql | pending | 2026-08-02 | (local draft, not yet committed) |  <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) — reconciled 2026-08-09 (see "2026-08-09 staging reality reconciliation" below); this row previously read DRAFT/NOT APPLIED, which had drifted from the live database. Verified read-only via PostgREST OpenAPI schema (service_role key, GET only): zero `messa_*` RPCs remain and all seven `mesa_*` RPCs (mesa_open_session_v1, mesa_release_empty_session_v1, mesa_open_reservation_v1, mesa_save_table_v1, mesa_post_payment_v1, mesa_save_reservation_v1, mesa_set_reservation_status_v1) are present — the cutover is complete on the DB side. NOTE: the currently-deployed backend (staging commit 9569023f8, 2026-07-30) predates all Mesa code entirely (no `mesa_*`/`messa_*` reference anywhere in that commit), so this DB-side cutover creates no live naming conflict with what's actually running. Pure rename, no behavior change: every messa_*_v1 function/trigger from V3-H/V3-H.1A/V3-H.2/V3-I is recreated under mesa_*_v1 (byte-identical body logic), every MESSA_* error code becomes MESA_* (including the two service-close guards' MESA_TABLES_NOT_RELEASED, previously MESSA_TABLES_NOT_RELEASED), the idempotency-scope tag becomes 'mesa_' and the ledger meta tag becomes 'source':'mesa'. Hard-guarded on the exact V3-I predecessor (messa_release_empty_session_v1/messa_guard_covers_monotonic_v1 must already exist) since the recreated bodies are V3-I's amended versions, not V3-H's originals. Drops every old messa_* function only after all triggers are repointed at the new mesa_* names, so no window ever has a trigger referencing a dropped function. See "V3-J cutover ordering" below for the required deploy sequence. -->
| 52 | 2026-08-02 → **V3-K MESA TABLE SHAPES** | 2026-08-02_v3k_mesa_table_shapes.sql | pending | 2026-08-02 | (local draft, not yet committed) |  <!-- DRAFT — NOT APPLIED to any database. Requires V3-J (mesa_save_table_v1 10-arg) applied first. Adds restaurant_tables.shape_preset (NOT NULL DEFAULT 'standard', backfills every existing row) with a CHECK enforcing exactly four valid (shape, shape_preset) combinations: round/standard, square/standard, rectangle/standard, rectangle/long. (An earlier draft also gave square a "rounded" preset -- five combinations -- but that was simplified out before commit per product review; round and square never needed a second look, only the rectangle's length is operationally meaningful.) Explicitly DROPs the old 10-arg mesa_save_table_v1 and CREATEs an 11-arg version (p_shape_preset text DEFAULT 'standard' appended last) -- a plain CREATE OR REPLACE would have added a second overload instead of truly replacing it. Server-side validation mirrors the DB CHECK exactly. -->
| 53 | 2026-08-08 → **SERVICE CLOSEOUT V2 / SLICE 1 (hardened in place by SLICE 3.2)** | 2026-08-08_service_closeout_incidents_foundation.sql | pending | 2026-08-08 | (local draft, not yet committed) |  <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) — reconciled 2026-08-09 (see "2026-08-09 staging reality reconciliation" below); this row previously read DRAFT/NOT APPLIED, which had drifted from the live database. Verified read-only via PostgREST (service_role key, GET only): `service_closeout_snapshots`, `service_incidents` and `service_incident_resolutions` tables all queryable on staging, and hold real incident-evidence rows: 4 incidents, 1 snapshot, tied to closeout_correlation_id c9ab9b97-6336-4ebe-bc9f-3913b78acd90 (stale PRANZO service_session 33174121-4f95-4ff5-a67e-aa5dde803d31, business_date 2026-08-07 — preserved evidence, not repaired by this reconciliation). SLICE 3.2: resolution_status vocabulary widened to add 'superseded' (a system-only disposition written exclusively by supersede_closeout_attempt in row 55's migration, never settable via resolve_service_incident, whose own vocabulary check is unchanged), and service_incidents_resolution_resolved_fields_chk widened to also require resolved_at/resolved_by/resolution_type for that status. Pure addition, no existing table altered: creates service_closeout_snapshots (immutable pre-close snapshot, append-only via trigger, idempotent on closeout_correlation_id — a retry returns the existing row, never a duplicate), service_incidents (persistent incident register spanning informational/operational/financial/integrity/security categories; detection facts immutable via service_incidents_facts_immutable, rows never deletable, resolution_* columns mutable only through resolve_service_incident, deduped on closeout_correlation_id+incident_type+entity via a NULL-safe unique index), and service_incident_resolutions (append-only resolution event log, source of historical resolution truth). All three RPCs (capture_closeout_snapshot/create_service_incident/resolve_service_incident) are SECURITY INVOKER, RLS-enabled with zero policies, service_role-only grants; resolve_service_incident additionally enforces role='admin' server-side, independent of any caller claim. order_id/table_session_id/giro_id/rider_id on service_incidents are deliberately FK-less historical-identity references (same reasoning as order_financial_events dropping ofe_order_id_fk). Nothing here is called by chiudiServizio, ensureServiceSession, guard_service_session_closed_v1, or any HTTP action — storage/RPC/idempotency foundation only, zero service-session rollover behaviour change. See docs/SERVICE_CLOSEOUT_V2_SLICE_2_FINANCIAL_RESOLUTION_DESIGN_NOTE.md for the (unimplemented) Slice 2 follow-on design. -->
| 54 | 2026-08-08 → **SERVICE CLOSEOUT V2 / SLICE 2 — POST-CLOSE FINANCIAL RESOLUTION (hardened in SLICE 2.1, SLICE 2.2)** | 2026-08-08_service_closeout_post_close_financial_resolutions.sql | pending | 2026-08-08 | eebf5ed95d15b5c9 |  <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) — reconciled 2026-08-09 (see "2026-08-09 staging reality reconciliation" below); this row previously read DRAFT/NOT APPLIED, which had drifted from the live database. Verified read-only via PostgREST (service_role key, GET only): `archived_order_financial_resolutions` table queryable on staging (0 rows — consistent with no financial resolution having been recorded yet). Implements the design docs/SERVICE_CLOSEOUT_V2_SLICE_2_FINANCIAL_RESOLUTION_DESIGN_NOTE.md sketched during Slice 1, hardened in place by SLICE 2.1 (modified directly since Slice 2 was never applied — see the migration's own SLICE 2.1 HARDENING header comment for the full rationale). Pure addition: creates archived_order_financial_resolutions, an append-only ledger recording what happened to an archived (storico) order's unpaid balance AFTER service close — recovered_payment, write_off, or a full reversal of one specific prior event in the same lineage ('correction' was investigated and deliberately folded into reversal+a fresh event rather than kept as a redundant 4th vocabulary term). Never rewrites storico, serata_summary, order_financial_events, or service_incidents.financial_exposure_cents — those stay frozen "truth at close" forever; this table only layers new append-only facts on top, read separately at report time. Lineage identity is the PAIR (service_session_id, archived_order_id) — NEVER archived_order_id alone, since storico's own uniqueness is UNIQUE(service_session_id, orden_id) and order numbers are reused across sessions/days; the RPC now also verifies the archived order actually exists in storico under that exact pair (ARCHIVED_ORDER_NOT_FOUND otherwise). SLICE 2.1: original_exposure_cents is no longer a caller-supplied parameter — the first event of a lineage derives it server-side from the linked service_incidents row's immutable financial_exposure_cents; related_incident_id is now NOT NULL and mandatory (was optional), validated to belong to the same service_session_id, category='financial', and the same archived order (INCIDENT_LINK_REQUIRED/INCIDENT_NOT_FOUND/INCIDENT_SERVICE_MISMATCH/INCIDENT_NOT_FINANCIAL/INCIDENT_ORDER_MISMATCH), and frozen per lineage exactly like original_exposure_cents (INCIDENT_LINK_MISMATCH on drift). A new lineage_sequence integer column (allocated 1,2,3,... while the lineage's advisory lock is held, enforced UNIQUE via aofr_lineage_idx on (service_session_id, archived_order_id, lineage_sequence)) is now the sole authoritative accounting order — the "current event" lookup uses ORDER BY lineage_sequence DESC, never created_at DESC or a uuid tiebreak. Idempotent on action_correlation_id (never order_id+amount — the same amount can legitimately be recovered twice across separate partial payments) via an early SELECT-and-return short-circuit that runs BEFORE any state-dependent validation/arithmetic — SLICE 2.1 fixed a genuine defect here found during its own real-Postgres validation: relying solely on ON CONFLICT DO NOTHING at INSERT time meant a retry submitted after the lineage had since advanced could be wrongly rejected (e.g. OVER_RESOLUTION_EXCEEDS_REMAINING) instead of returning the original event; ON CONFLICT DO NOTHING is kept as a genuine-concurrent-race backstop. SLICE 2.2: that early short-circuit is now bound to the exact command, not just the correlation id — every command-defining field (service_session_id, archived_order_id, related_incident_id, resolution_type, amount_cents, payment_method, reversed_event_id) is compared against what was actually persisted; a correlation id reused for a genuinely different command returns ACTION_CORRELATION_ID_CONFLICT (naming mirrors capture_closeout_snapshot's CLOSEOUT_CORRELATION_ID_CONFLICT from Slice 1) instead of silently handing back somebody else's recorded event; actor/reason/note are deliberately excluded from the comparison as attribution/narrative metadata. A pg_advisory_xact_lock keyed on the lineage pair still serializes concurrent writers, including the very first event. remaining_exposure_cents is computed server-side per event; recovered_payment/write_off fail closed with OVER_RESOLUTION_EXCEEDS_REMAINING rather than clamp; reversal is full-amount-only and a given event can be reversed at most once. created_at uses clock_timestamp(), not now() (Slice 2's own real-Postgres finding) but is audit metadata only, never the ordering primitive (that's lineage_sequence, per SLICE 2.1). Integer cents throughout (never numeric/float) — a deliberate, documented unit difference from order_financial_events' decimal-euro amount column, never silently unified here. The one RPC (create_archived_order_financial_resolution) is SECURITY INVOKER, RLS-enabled with zero policies, service_role-only SELECT+INSERT grants (REVOKE ALL FROM service_role first, per the Slice 1.3 deterministic-privilege-floor discipline, applied from the start on this table), and enforces role='admin' server-side as defense-in-depth, independent of any caller claim (same Slice 1.1 trust boundary — not real authentication, no public HTTP path exists yet). archived_order_id carries no foreign key (same FK-less historical-identity philosophy as order_financial_events.order_id/service_incidents.order_id). Nothing here is called by chiudiServizio, order_mark_paid/order_refund/order_void, or any HTTP action — storage/RPC/idempotency/arithmetic foundation only, zero change to any existing financial RPC or service-session rollover behaviour. -->
| 55 | 2026-08-08 → **SERVICE CLOSEOUT V2 / SLICE 3.1 — ATTEMPT OWNERSHIP (hardened in place by SLICE 3.2)** | 2026-08-08_service_closeout_attempt_ownership.sql | pending | 2026-08-08 | (local draft, not yet committed) |  <!-- SLICE 3.2: supersede_closeout_attempt now atomically (same function body/transaction) also transitions the superseded attempt's still-pending/acknowledged service_incidents rows to resolution_status='superseded' — facts/original snapshot/exposure untouched, never deleted. APPLIED ON STAGING (tdikhfeinufaahagmpjz) — reconciled 2026-08-09 (see "2026-08-09 staging reality reconciliation" below); this row previously read DRAFT/NOT APPLIED, which had drifted from the live database. Verified read-only via PostgREST (service_role key, GET only): `service_closeout_attempts` table queryable on staging, holding one real active attempt (closeout_correlation_id c9ab9b97-6336-4ebe-bc9f-3913b78acd90, service_session_id 33174121-4f95-4ff5-a67e-aa5dde803d31, status=active, started_at 2026-08-09T11:59:44Z — created by the legacy deployed backend's boot-time catch-up firing at that restart; preserved as incident evidence, not repaired here). Requires Slice 1 (row 53) applied first (confirmed also applied). Pure addition: creates service_closeout_attempts, the authoritative persistent ledger for "which closeout attempt currently owns this service session's rollover" — fixes two defects found auditing the (unwired, JS-only) Slice 3 orchestrator: (a) two truly concurrent callers with no existing snapshot yet could each mint their own closeout_correlation_id and both succeed at capture_closeout_snapshot (idempotent only on their OWN id, never on service_session_id), producing two snapshots for one session; (b) once ANY snapshot existed for a session, Slice 3 replayed its frozen classification forever, even after an operator corrected exactly what it complained about, so a genuinely corrected service could never get an accurate second attempt. service_closeout_attempts_active_uq (a partial UNIQUE index on service_session_id WHERE status='active') makes "at most one active attempt per session" a database invariant, not an application convention; acquire_closeout_attempt(session, actor) is the ONLY minter of closeout_correlation_id (get-or-create, race-safe purely via that index — two concurrent INSERTs serialize at the database, the loser's ON CONFLICT DO NOTHING finds and returns the winner's row); supersede_closeout_attempt(id, actor, reason) idempotently retires an attempt whose frozen snapshot no longer matches a fresh state-fingerprint comparison (computeStateFingerprint in rolloverClassifier.js — a sha256 over the same raw orders/table_sessions/order_financial_events rows the classifier itself reads, never elapsed time); complete_closeout_attempt(id, actor) idempotently marks an attempt terminal-successful only after chiudiServizio has actually closed the session it owns. No superseded_by forward pointer — full attempt lineage for a session is already reconstructable from (service_session_id, started_at) without one. All three RPCs are SECURITY INVOKER, RLS-enabled with zero policies, service_role-only SELECT+INSERT+UPDATE grants (REVOKE ALL first, Slice 1.3 discipline); rows are never deleted (dedicated no-delete trigger) and only the five lifecycle-transition columns (status/completed_at/superseded_at/supersession_reason/updated_at) can ever change post-insert (deny-by-default guarded-transitions trigger, same pattern as service_incidents_immutable_facts), with completed/superseded both enforced as dead-end terminal states independent of the RPC bodies. Nothing here is called by chiudiServizio or any HTTP action directly — incidentSafeRollover.js (JS, same commit) is the sole caller of all three RPCs. -->
| 56 | 2026-08-09 → **SERVICE CLOSEOUT V2 / SLICE 4C.2C — CROSS-SERVICE TABLE POLICY + AUTO-RESOLUTION ORDERING** | 2026-08-09_service_closeout_cross_service_table_policy.sql | pending | 2026-08-09 | ce409a48f88904dc |  <!-- UNAPPLIED — V2 legacy-policy migration; do not deploy pending V3 replacement. Confirmed NOT applied on staging (tdikhfeinufaahagmpjz) as of 2026-08-09 via read-only PostgREST schema check (service_role key, GET only): this migration's one new, uniquely-identifying RPC, `mesa_release_empty_session_auto_v1`, is absent from the live schema (the CREATE OR REPLACE revisions to begin_service_session_close/guard_service_session_closed_v1 have unchanged signatures and so cannot be probed directly, but the new RPC's absence is sufficient — the migration is a single transaction). Requires rows 53/55 applied first (both confirmed applied). This migration's own guard refuses to run if service_sessions/service_closeout_attempts/service_incidents/table_sessions or begin_service_session_close/guard_service_session_closed_v1/supersede_closeout_attempt/mesa_release_empty_session_v1 are missing). Closes the gap found on the first real staging attempt after Slice 4C.1's JS-level chiudiServizio() gate fix: begin_service_session_close() and guard_service_session_closed_v1 each had their OWN independent open-table check, so fixing only the JS gate just moved the failure. Both are revised (CREATE OR REPLACE, unchanged signature/grants) to skip their open-table check ONLY when an active row exists in service_closeout_attempts for that exact service_session_id — the one signal only acquire_closeout_attempt() (HTTP-unreachable) can ever produce, so no request body/querystring can forge it; manual close with no active attempt is completely unchanged. Also creates mesa_release_empty_session_auto_v1(uuid,uuid) — a trusted-system sibling of mesa_release_empty_session_v1 with the identical session/covers checks minus the human-actor authorization block that RPC correctly requires for its real (human/Mesa-UI) caller; service_role-only, never HTTP-exposed. And widens supersede_closeout_attempt's existing incident-reconciliation cascade to also catch a resolution_status='resolved'/resolution_type='auto_released_empty_table' row (an unverified system claim) alongside the existing pending/acknowledged catch — a genuine admin resolution (any other resolution_type) stays permanently protected. Paired with the same-date JS fix (incidentSafeRollover.js/mesaDao.js/supabaseResourcePolicy.js): an EMPTY_TABLE_LEFT_OPEN incident is now always created pending and only transitions to resolved via resolve_service_incident AFTER the release RPC call actually confirms success — closing a real staging defect where it was previously recorded resolved before the release attempt even ran. Rollback restores all three functions to their exact pre-4C.2C bodies and drops the new RPC; it does not and must not un-happen any row already affected by the new behavior. -->
| 57 | 2026-08-09 → **SERVICE LIFECYCLE V3 / SLICE 3.1 — DATA FOUNDATION** | 2026-08-09_service_lifecycle_v3_foundation.sql | 4252241 | 2026-08-09 | cf2d1699527828b4 |  <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) — applied 2026-08-10 (V3.5 controlled staging cutover, Phase 3), via Supabase MCP apply_migration, following MIGRATION_MANIFEST.md apply_order 57→58→59→60→61→62 (row 56 excluded). Post-apply verification: service_closeouts exists with 30 columns; mesa_prepare_table_order_v1's real executable body (pg_get_functiondef, comment-stripped) confirmed no longer sets service_session_id; protected stale PRANZO (id 33174121-4f95-4ff5-a67e-aa5dde803d31) status/incident-count(4)/current-pointer confirmed unchanged before and after. PARSED + STATICALLY VALIDATED prior to apply. Does NOT depend on row 56 (retired) and does not touch any Service Closeout V2 table (service_closeout_attempts/service_closeout_snapshots/service_incidents/service_incident_resolutions/archived_order_financial_resolutions — all adopted AS-IS by V3.1, see SERVICE_LIFECYCLE_V3_1_FOUNDATION_REPORT.md). Two parts in one file: PART 1 fixes a real order-attribution defect (mesa_prepare_table_order_v1, firing before ordenes_assign_service_session alphabetically, was setting a new order's service_session_id to the TABLE's origin session instead of leaving it to the current-session assignment trigger — raises SERVICE_SESSION_FORGERY for any order placed on a table that has legitimately crossed a service boundary, the exact accepted cross-service Mesa contract ae4c3f9 established at the close side). CREATE OR REPLACE removes exactly the one offending assignment line — guarded by a predecessor-body check, proven byte-for-byte identical otherwise by static diff against the live 2026-08-02_v3j_mesa_nomenclature_cutover.sql body. PART 2 creates service_closeouts: the ONE authoritative, append-only, immutable frozen result of a successful service close (distinct from the attempt/snapshot/incident list/serata_summary/storico), integer-cents financial facts (unpaid_exposure_cents frozen forever, matching the real 5750-cent stale-PRANZO evidence as its canonical worked example), operational facts, incident count aggregates only (original service_incidents rows stay canonical), fiscal/cash-drawer extension points documented not implemented. Schema-only: no RPC, no HTTP action, no chiudiServizio call — V3.2's close engine is the first thing that will INSERT a row. Validation performed: (a) outer SQL grammar parsed successfully offline against the real PostgreSQL parser (libpg-query, no DB connection); (b) 61/61 static structural tests (tests/serviceLifecycleV3Foundation.static.test.js) covering uniqueness/immutability/attempt-linkage/financial-frozen-truth/table-cross-service/order-attribution/no-destructive-dependency/access-control/rollback-mirror; (c) 13/13 JS behavioural tests for the read-only serviceCloseouts.js wrapper; (d) one genuine live empirical probe against staging (tdikhfeinufaahagmpjz, explicit owner authorization, single-request scope) confirmed the order-attribution trigger chain is real and fails closed (hit the pre-existing STALE_SERVICE_SESSION gate before reaching SERVICE_SESSION_FORGERY specifically — see report). NOT executed: no direct Postgres connection or DDL-capable tool was available in this environment (no `pg` driver, no psql, no local Postgres/Docker, PostgREST does not execute arbitrary DDL) — this migration's exact SQL has never actually been run against any Postgres instance, live or local. This is a reported tooling limitation, not a skipped step. -->
| 58 | 2026-08-09 → **SERVICE LIFECYCLE V3 / SLICE 3.2 — AUTHORITATIVE CLOSE ENGINE (HAPPY PATH ONLY)** | 2026-08-09_service_lifecycle_v3_close_engine.sql | 5152055 | 2026-08-09 | 1246fd5abf816520 |  <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) — applied 2026-08-10 (V3.5 controlled staging cutover, Phase 3), via Supabase MCP apply_migration. Post-apply verification: create_service_closeout and close_service_session_v3 each resolve to exactly one pg_proc overload; guard_service_session_closed_v1's real executable body (comment-stripped) contains the service_closeouts exemption and still checks MESA_TABLES_NOT_RELEASED; service_role-only EXECUTE confirmed (anon denied); protected stale PRANZO status/incident-count(4)/current-pointer confirmed unchanged before and after. STATICALLY VALIDATED prior to apply. NEW ENGINE, NO LEGACY CLOSEOUT: does not call chiudiServizio, begin_service_session_close, or complete_service_session_close, does not touch storico/serata_summary, and does not depend on retired row 56. Requires row 57 applied first. Three parts in one file: PART 1 creates create_service_closeout(...) — the ONLY writer of row 57's service_closeouts, idempotent via the exact ON CONFLICT (closeout_correlation_id) DO NOTHING + re-fetch recipe capture_closeout_snapshot already established, requiring an ACTIVE service_closeout_attempts row scoped to the exact (session, correlation) pair; business_date/service_kind/opened_at are server-derived from service_sessions, never caller-supplied; total_discounts_cents is always 0 (no clean canonical discount source in this slice, not fabricated). PART 2 creates close_service_session_v3(...) — the V3-native terminal transition (service_sessions -> 'closed'), replacing the legacy begin/complete_service_session_close two-phase dance the engine is forbidden from calling; reuses the SAME 'service_session_lifecycle' advisory-lock namespace as the legacy RPCs (cross-engine mutual exclusion on the shared service_session_state singleton); idempotent (a retry after this RPC already succeeded returns ALREADY_CLOSED, mirroring complete_service_session_close's own idempotent branch); requires service_closeouts to already exist for the exact (session, correlation) pair AND an active attempt before it will transition anything. PART 3 — the one genuinely delicate piece — CREATE OR REPLACEs guard_service_session_closed_v1 (the BEFORE UPDATE OF status trigger every service-close write fires, regardless of caller) with ONE new, narrowly-scoped exemption: its MESA_TABLES_NOT_RELEASED check is now skipped ONLY when a service_closeouts row already exists for that exact session — proof PART 1 already ran, under a real active V3 attempt, with server-computed totals. Deliberately NOT gated on service_closeout_attempts (which V2's incidentSafeRollover.js/row 56 also read+write, and which would therefore silently re-open the LEGACY path's own open-table block the moment an attempt happened to be active) — service_closeouts has never had a writer before PART 1 (row 57's own note: "V3.2's close engine is the first thing that will INSERT a row") and is service_role-only INSERT, never HTTP-exposed, so the legacy chiudiServizio/begin_service_session_close path can never satisfy this EXISTS check; its own open-table policy is completely unchanged. The SERVICE_ACTIVE_ORDERS_NOT_RESOLVED half of the same trigger is left fully unconditional — untouched — proven byte-identical to the live body by static diff. A predecessor-body guard (same discipline as row 57's mesa_prepare_table_order_v1 fix) refuses to apply over a drifted or already-patched function. Both new RPCs are SECURITY INVOKER, RLS is inherited from service_closeouts/service_sessions (no new table here), service_role-only EXECUTE grants (REVOKE ALL first). Validation performed: (a) 46/46 static structural tests (tests/serviceLifecycleV3CloseEngineMigration.static.test.js) covering staging safety, additive-only/no-new-table, no legacy DML, both new RPCs' idempotency/identity checks, the trigger predecessor-body diff proof (code-identical, comment-stripped SERVICE_ACTIVE_ORDERS_NOT_RESOLVED block, subsequence-preserving MESA_TABLES_NOT_RELEASED wrap), grants, and rollback mirror; (b) 45/45 behavioural tests for src/serviceSessions/serviceLifecycleEngine.js (tests/serviceLifecycleV3CloseEngine.test.js) against fake dependencies — empty service, fully-paid service, occupied-table-survives-close, retry-after-partial-crash-creates-no-duplicate (including the "closed by something else, zero orders" fabrication guard), concurrent-callers-converge-on-one-closeout, unpaid/non-terminal anomaly refused with zero mutation, order-attribution regression (current-service scoping, not table-origin); (c) 27/27 in a dedicated legacy non-interference static scan (tests/serviceLifecycleV3EngineLegacyNonInterference.static.test.js, comment-stripped) proving the new engine's own files never reference chiudiServizio/storico/serata_summary/scheduleDeferredCloseRetry/begin or complete_service_session_close; (d) 4/4 real-transport (non-DI) resource-policy integration test (tests/serviceLifecycleV3CloseEngineResourcePolicyIntegration.test.js) plus a widened tests/supabaseResourcePolicy.test.js (99/99 total); (e) tests/serviceLifecycleV3Foundation.static.test.js's own "application wiring" check (17a) was deliberately widened (61/61, still green) to recognize serviceCloseoutCreation.js/serviceLifecycleEngine.js/supabaseResourcePolicy.js as the now-sanctioned V3.2 write path, exactly as that row's own comment predicted. NOT executed: same tooling limitation as row 57 — no `pg` driver, no psql, no local Postgres/Docker available in this environment, so this migration's exact SQL (including the trigger CREATE OR REPLACE) has never been run against any real Postgres instance. Opening the next service is explicitly out of scope for this slice; anomaly/incident classification is deferred to V3.3. -->
| 59 | 2026-08-09 → **SERVICE LIFECYCLE V3 / SLICE 3.2.1 — CLOSE OWNERSHIP + RETRY HARDENING** | 2026-08-09_service_lifecycle_v3_close_ownership_hardening.sql | 77279d6 | 2026-08-09 | 69366f98d4d234c6 |  <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) — applied 2026-08-10 (V3.5 controlled staging cutover, Phase 3), via Supabase MCP apply_migration. Post-apply verification: close_service_session_v3's real executable body (comment-stripped) sets the ladieci.v3_close_authorized_session_id marker via set_config(...); guard_service_session_closed_v1's real body checks that marker AND the service_closeouts row; both functions single-overload; protected stale PRANZO status/incident-count(4)/current-pointer confirmed unchanged before and after. VALIDATED prior to apply. Hardens row 58 only (requires it applied first); no new table, CREATE OR REPLACE of the same two row-58 functions only. TWO issues fixed. ISSUE A (JS-only, no migration content): serviceLifecycleEngine.js's order-count ownership heuristic (`alreadyClosed && orders.length === 0` → NOT_RECOVERABLE) removed entirely and replaced with an explicit lineage state machine driven ONLY by service_closeouts/service_closeout_attempts (new closeoutAttempts.js `getByCorrelationId()`, real serviceCloseouts.js `getBySessionId()` now wired in), resolving Cases A-F from the session spec before Phase A ever runs — a zero-order session now closes and retries identically to any other. ISSUE B (this migration's SQL content): guard_service_session_closed_v1's row-58 occupied-table exemption (`NOT EXISTS(service_closeouts)`) was scoped to "a closeout row exists for this session" when it needed to be scoped to "THIS UPDATE is the authorized V3 transition" — a later, separate direct service-role UPDATE could otherwise silently inherit the exemption forever once any closeout row existed. Fix: close_service_session_v3 now calls `PERFORM set_config('ladieci.v3_close_authorized_session_id', v_session.id::text, true)` (transaction-local, is_local=true) immediately before its terminal UPDATE; the trigger's exemption now requires BOTH that marker (matching OLD.id exactly) AND the service_closeouts row — no HTTP-reachable parameter, new grant, or role influences either. Validation performed: (a) 49/49 static structural tests (tests/serviceLifecycleV3CloseOwnershipHardening.static.test.js) — line-for-line subsequence diff proving PART 1 is exactly one inserted line vs row 58's close_service_session_v3 body, comment-stripped code-identity of guard_service_session_closed_v1's untouched SERVICE_ACTIVE_ORDERS_NOT_RESOLVED half, rollback byte-identical to both row-58 bodies, GUC-name-literal match between the set_config/current_setting call sites (typo guard), no HTTP-reachable override parameter; (b) 50/50 new behavioural tests (tests/serviceLifecycleV3CloseOwnershipRetry.test.js) against fake dependencies covering every named case — H: zero-order close then exact idempotent retry (the literal regression case for the removed heuristic, proven via a real prior-generation bug: see (d) below), I: CASE B resume (seeded closeout+active-attempt, session still open), J: CASE D resume (session already closed), K: CASE C pure read-only short-circuit (zero RPC calls), L: CASE E refusal regardless of order count (zero AND non-zero orders), M: CASE E with an unrelated stray active attempt (never touched, never fabricates), N: concurrent retry converges with zero duplicate calls, O: fail-closed on three structurally-impossible-but-defended lineage inconsistencies; (c) tests/serviceLifecycleV3CloseEngine.test.js's own Scenario D re-verified and strengthened (D5/D8: retry now provably short-circuits via lineage — create()/acquire() each called exactly once total, not once per call); (d) REAL POSTGRES VALIDATION against staging (tdikhfeinufaahagmpjz, confirmed distinct from production wnswassgfuuivmfwjxsf, confirmed via the staging sentinel migration) inside BEGIN transactions that were NEVER committed (empirically proven twice over — an explicit-ROLLBACK probe and a no-COMMIT-ends-on-SELECT probe both left zero trace) — applied rows 57+58+59 together successfully multiple times, proving the DDL is valid, executable Postgres 17, and in the process found and fixed a REAL bug: row 59's own predecessor-body guard for close_service_session_v3 compared pg_get_function_identity_arguments() against parameter names missing their real `p_` prefix, which would have made this migration permanently refuse to apply ("body not found") had it been submitted as originally drafted — caught only by real execution, not by the static suite, and fixed before this file's final version. Discovered mid-session that (i) service_sessions has TWO partial unique indexes invisible to a pg_constraint-only query (service_sessions_single_active_uq: at most one 'open'/'closing' row system-wide; service_sessions_date_kind_uq: unique business_date+service_kind regardless of status) and table_sessions has one more (table_sessions_one_open_per_table_uq), and (ii) a REAL session (id 33174121-4f95-4ff5-a67e-aa5dde803d31, business_date 2026-08-07, PRANZO — the known stale PRANZO) currently and permanently occupies that single active slot. Per explicit instruction this row was NEVER read inside a mutating statement, NEVER updated, and NEVER referenced by any synthetic fixture (confirmed via a read-only audit before AND after every attempt: status/updated_at unchanged throughout); every synthetic id used the fixed '00000000-0000-4000-8000-00000000000X' pattern with fake '2099-*' business_dates, and every synthetic service_sessions row was inserted already 'closed' (bypasses the trigger, which only fires on UPDATE) specifically so no synthetic fixture could ever contend for the stale row's slot. The full 7-scenario security-test batch (T1-T7, matching the session's required test list) could not be brought to a single clean end-to-end pass in the time available: this is a LIVE, currently-operational staging environment where real table occupancy changes over time (actual seated/freed tables), and repeated attempts collided with genuinely-changing real table_sessions occupancy on the physical tables borrowed read-only for FK satisfaction, not with leftover synthetic state (re-confirmed clean via read-only audit after every single attempt, including the last). This is a reported, honest environmental limitation, not a faked pass — the core hardening property (the exemption denies a direct UPDATE even when a real service_closeouts row exists, per Security Test 3) was successfully exercised at least once via an isolated shadow-table technique (the real trigger FUNCTION attached to a throwaway temp table, its internal queries still hitting the real service_closeouts/table_sessions tables) before the session ended without a final consolidated clean run; the case-by-case logic is otherwise proven exhaustively by (a)-(c) above. Manual/legacy close behaviour (the trigger's original unconditional path) is completely untouched by this migration and was not re-tested. -->
| 61 | 2026-08-09 → **SERVICE LIFECYCLE V3 / SLICE 3.3 — INCIDENT/ANOMALY POLICY** | 2026-08-09_service_lifecycle_v3_incident_policy.sql | 49c4839 | 2026-08-09 | 320fd694de9129d7 |  <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) — applied 2026-08-10 (V3.5 controlled staging cutover, Phase 3), via Supabase MCP apply_migration, AFTER row 60 (table_release) supplied mesa_release_empty_session_auto_v1 under V3's own ownership — this migration's guard was satisfied by that source, exactly as row 60's own entry documents; row 56 stayed excluded throughout. Post-apply verification: create_service_closeout resolves to exactly ONE pg_proc overload with the full 23-parameter signature (the row-58 18-parameter overload confirmed gone — the exact overload-ambiguity bug this migration's DROP+CREATE discipline exists to prevent); service_role-only EXECUTE confirmed; protected stale PRANZO status/incident-count(4)/current-pointer confirmed unchanged before and after. DRAFT-validated prior to apply. Requires rows 57-59 applied first (guard-checked), PLUS row 53's `create_service_incident`/`resolve_service_incident` (already live on staging per row 53's own note) AND row 56's `mesa_release_empty_session_auto_v1` — row 56 itself is currently UNAPPLIED on staging (deliberately, per the 2026-08-09 V3 staging freeze), so this migration cannot actually be applied yet; that ordering gap is a real, open precondition, not resolved by this migration. Sole SQL change: `create_service_closeout` (row 58's 18-parameter function) gets five new trailing DEFAULT-0 aggregate-count parameters (kitchen/listo/delivery-pending counts, incident_count, critical_incident_count) so serviceLifecycleEngine.js can freeze incident-fact counts onto the one authoritative service_closeouts row (columns have existed with DEFAULT 0 since row 57). CRITICAL CORRECTNESS FIX made during this session's recovery: the migration as left by the interrupted prior session used a bare `CREATE OR REPLACE FUNCTION` for the new 23-parameter signature — proven empirically against staging (tdikhfeinufaahagmpjz), via a synthetic scratch function inside BEGIN/ROLLBACK mirroring the exact same shape, that PostgreSQL function identity is (name, input-parameter TYPE list) only, never names/defaults; adding trailing DEFAULT parameters therefore does NOT replace the existing 18-parameter function — it creates a SECOND, overloaded pg_proc row, and a call using the OLD parameter count (positional OR named — PostgREST's own call shape) then fails at runtime with `42725: function ... is not unique`, breaking every caller, old or new, the instant it lands. Fixed to an explicit `DROP FUNCTION` of the proven-present 18-parameter signature (guarded immediately above by an exact identity+body match) followed by `CREATE FUNCTION` of the 23-parameter one, re-granting service_role EXECUTE explicitly (a DROP destroys the dropped function's own grants). Re-verified empirically the same way: exactly one pg_proc row afterward, and both the old 18-arg call shape (falling through to the new trailing defaults) and the new 23-arg shape resolve unambiguously. Paired `.ROLLBACK.sql` added (was missing from the interrupted WIP) mirroring the same DROP+CREATE discipline in reverse. JS-side (src/serviceSessions/v3IncidentPolicy.js — new, pure classification/policy module with zero DB access and zero coupling to the V2-adjacent rolloverClassifier.js/incidentSafeRollover.js, reusing only their proven incident-type VOCABULARY; src/serviceSessions/serviceLifecycleEngine.js, src/closeout/serviceCloseoutCreation.js) removes the Slice 3.2 happy-path-only refusal (`V3_CLOSE_UNSUPPORTED_NON_HAPPY_PATH`) entirely: every non-hard anomaly (non-terminal order, unpaid balance, a truly-empty open table — `covers_total IS NULL`) now classifies into a persisted, non-blocking `service_incidents` row (idempotent via row 53's own dedupe unique index — a retry under the same active attempt never duplicates) before the closeout is created; an OCCUPIED open table is never an incident (unchanged operational fact only); a failure to durably persist a REQUIRED incident is itself a new hard blocker (`V3_CLOSE_INCIDENT_PERSISTENCE_FAILED`) with no closeout ever created; an empty table is auto-released via row 56's `mesa_release_empty_session_auto_v1` and its incident marked resolved ONLY after that release RPC confirms success (never at creation time) — a failed release leaves the incident pending, never falsely resolved, and is non-fatal to the close itself. Also fixed during recovery: the engine's own `classify()` call is now wrapped in try/catch, translating v3IncidentPolicy.js's own fail-closed guard (an incident type with no policy entry — a defect, not a business scenario) into a controlled `V3_CLOSE_CLASSIFICATION_FAILED` result instead of an unhandled promise rejection. Validated with 2 new test files: tests/v3IncidentPolicy.test.js (56 pure classifier assertions) and tests/serviceLifecycleV3IncidentPolicyEngine.test.js (64 engine-integration assertions, lettered A-L matching the session's own required-case list) — both new, plus tests/serviceLifecycleV3CloseEngine.test.js's Scenarios F/F2 updated to match the new (classify-and-close, not refuse) contract. Full existing backend suite (214 test files) re-run after these changes: 213 pass, 1 pre-existing unrelated baseline failure (tests/getOrdenesArchivadosSesionAuthorizationParity.test.js — untouched by any file this slice or its predecessor touched, confirmed via `git diff HEAD --stat` against its exact input files). NOT executed against real Postgres beyond the synthetic overload probes above: the migration's own guards refuse to apply while row 56 is unapplied, so a real end-to-end apply of THIS migration was never attempted (would touch row 56's precondition, out of this session's scope). ── V3.4 SESSION UPDATE (Gate 0): the row-56 precondition noted above was confirmed as a REAL blocker, empirically (staging tdikhfeinufaahagmpjz, real Postgres, BEGIN/ROLLBACK, zero residue) — applying rows 57→58→59 cleanly and then this migration, with row 56 correctly excluded, failed at exactly the `mesa_release_empty_session_auto_v1 missing` guard. Fixed: row 60 (2026-08-09_service_lifecycle_v3_table_release.sql) now supplies that one RPC verbatim under V3's own ownership (zero row-56 dependency); this migration's guard error message was corrected to point there instead (its check logic is unchanged — still a plain existence check, satisfied by either source). Re-verified empirically: the full chain 57→58→59→60→61 now applies cleanly end to end, zero residue. See row 60's own entry for the full fix rationale. -->
| 60 | 2026-08-09 → **SERVICE LIFECYCLE V3 / SLICE 3.4 — TABLE RELEASE (row 56 V3 replacement)** | 2026-08-09_service_lifecycle_v3_table_release.sql | 78ff66d | 2026-08-09 | ba2690b78a7be07f |  <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) — applied 2026-08-10 (V3.5 controlled staging cutover, Phase 3), via Supabase MCP apply_migration, BEFORE row 61 (incident_policy), satisfying its mesa_release_empty_session_auto_v1 dependency exactly as designed. Post-apply verification: mesa_release_empty_session_auto_v1 exists, single overload, service_role-only EXECUTE (anon denied); protected stale PRANZO status/incident-count(4)/current-pointer confirmed unchanged before and after. DRAFT-validated prior to apply. Requires nothing from row 56 (2026-08-09_service_closeout_cross_service_table_policy.sql, retired — "do not deploy pending V3 replacement", per that row's own note) — this migration IS that replacement, for the one piece of row 56 anything downstream (row 61) actually needs. Gate-0 finding this V3.4 session: row 61's guard has always required `mesa_release_empty_session_auto_v1` to exist, and row 56 is the only migration that ever created it — but row 56 is deliberately excluded from the applied set (2026-08-09 V3 staging freeze), and its OTHER two changes (begin_service_session_close/guard_service_session_closed_v1 exemption widened on an active service_closeout_attempts row, and supersede_closeout_attempt's auto-resolution reconciliation widening) are V2-orchestrator-specific (incidentSafeRollover.js) and were already independently superseded for the V3 close path by row 58 PART 3 / row 59's own transaction-local marker mechanism — V3 needs neither. Fix: extract ONLY `mesa_release_empty_session_auto_v1` verbatim (byte-identical body/signature/grants, proven by a static byte-equality test against row 56's own text — tests/serviceLifecycleV3TableReleaseMigration.static.test.js, 32/32) into this standalone migration, with zero reference to row 56, begin_service_session_close, guard_service_session_closed_v1, or supersede_closeout_attempt. Same RPC name, so no JS-side caller (src/tables/mesaDao.js releaseEmptySessionAuto()) needs to change. Validation performed: (a) 32/32 static structural tests; (b) REAL POSTGRES VALIDATION against staging (tdikhfeinufaahagmpjz, confirmed distinct from production wnswassgfuuivmfwjxsf) inside BEGIN/ROLLBACK, zero residue confirmed after every probe — proved (i) rows 57→58→59 apply cleanly against the live current schema ending in the exact expected shape (single overloads, 30-column service_closeouts, row-59 marker present), (ii) row 61's ORIGINAL guard fails with exactly `mesa_release_empty_session_auto_v1 missing` when row 56 is excluded (the concrete incompatibility, reproduced on demand), (iii) with this migration's function present, row 61's FIXED guard passes cleanly and the full chain 57→58→59→60→61 completes end to end. Rollback drops only the one function; touches nothing row 56 also touches. -->
| 62 | 2026-08-09 → **SERVICE LIFECYCLE V3 / SLICE 3.4 — NEXT-SERVICE OPENING + CARRYOVER** | 2026-08-09_service_lifecycle_v3_rollover.sql | 78ff66d | 2026-08-09 | 473c8b651321c6c8 |  <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) — applied 2026-08-10 (V3.5 controlled staging cutover, Phase 3, LAST migration in the chain), via Supabase MCP apply_migration. Post-apply verification: service_sessions.rollover_source_session_id column + service_sessions_rollover_source_uq partial unique index both exist; ensure_next_service_session_v3 exists, single overload, service_role-only EXECUTE (anon denied); protected stale PRANZO's own rollover_source_session_id confirmed NULL (never a rollover continuation); status/incident-count(4)/current-pointer confirmed unchanged before and after; service_sessions_single_active_uq slot confirmed still occupied by exactly the protected stale PRANZO (active_session_count=1) — the structural fact that blocks any fresh public mutating E2E fixture, per this session's Phase 8 determination. DRAFT-validated prior to apply. Requires rows 58/59 applied first (guard-checked); does not require or reference row 56 or row 60 directly (row 60 is required only by row 61, which this migration does not touch). Completes the NEW V3 rollover: acquire→snapshot→reconcile→incidents/safe-actions→closeout A→close A (unchanged through V3.3) →**ensure/reuse next current service B**→carryover summary→complete rollover attempt. SCHEMA: one additive nullable column `service_sessions.rollover_source_session_id` (FK to service_sessions(id) ON DELETE RESTRICT) plus its own partial UNIQUE index `service_sessions_rollover_source_uq` — explicit, permanent provenance ("this session is B, the V3 rollover continuation of that A"), read FIRST on every ensure attempt so a retry can find an already-created B without ever re-deriving from the clock (which may have crossed a schedule boundary since the original attempt). RPC: `ensure_next_service_session_v3(p_source_session_id, p_service_kind, p_business_date, p_opened_by, p_source)` — atomic, advisory-locked on the same 'service_session_lifecycle' namespace every V3/legacy close RPC already shares; idempotent by provenance first, then by the pre-existing `service_sessions_date_kind_uq` (business_date, service_kind) as the ON CONFLICT target (ON CONFLICT re-verifies provenance before ever claiming REUSED — never silently annexes an unrelated session); requires `service_session_state.recent_closed_session_id = p_source_session_id` (unforgeable lineage, only close_service_session_v3 ever writes that column) and `current_session_id IS NULL` (never overwrites a live pointer) before inserting. JS-SIDE: new pure module `src/serviceSessions/v3NextServiceIdentity.js` wraps the pre-existing, already-canonical `src/schedule/serviceSchedule.js` (zero window-boundary duplication) — `deriveNextServiceIdentity(now)` answers "should anything be current right now, and what" purely from the actual wall clock; `shouldEnsure:false` (the 17:30-18:00 buffer, the 00:00-08:00 overnight span) is a complete, successful rollover outcome, never a deferred one, and is NEVER a missed-boundary catch-up guess. `serviceLifecycleEngine.js` extended (not duplicated — one engine) with Phase F0 (ensure/reuse B, REQUIRED — a failure leaves the attempt active/recoverable, same posture as V3.3's incident-persistence gate), Phase F1 (carryover summary — read-only, no mutation: an open table's origin is never rewritten, per V3.1, so this only reports what already, structurally, carried over), Phase G (attempt completion, now gated on F0/F1). CASE C (already-completed rollover) is strictly read-only — looks up B by provenance, NEVER re-derives from today's clock, so a later read can never retroactively open a session a finished rollover already decided against. Also extended: `serviceLifecycleV3Transition.js` (ensureNext() wrapper; `publicSession()` gained `rolloverSourceSessionId`) and `supabaseResourcePolicy.js` (rpc/ensure_next_service_session_v3, service_role-only, POST-only, no HTTP action). GATE 0 (this session, before any implementation): the full intended chain (57→58→59→60→61) was validated empirically against staging inside BEGIN/ROLLBACK before this migration was even designed — see row 60's own entry. POSTGRES VALIDATION for THIS migration, same method, zero residue after every probe: (a) idempotent-reuse-by-provenance, lineage-check failure, current-already-set failure, and all five input-validation guards — proven directly against the real function; (b) the create+ON-CONFLICT-reuse+identity-conflict path — proven via an isolated temp-table shadow replica (the real table's OWN `service_sessions_single_active_uq` correctly makes a live create-test impossible while the real stale PRANZO stays open — the SAME class of environmental limitation the V3.2.1 session already documented for this exact constraint, not a new one); (c) **the post-boundary order-attribution invariant** (Step 4 — the single most important fact in this slice: a NEW order on a carried-over table gets B, never A, and a caller-supplied mismatch still fails closed with SERVICE_SESSION_FORGERY) — proven via a second isolated shadow replica of `ordenes_assign_service_session`'s exact decision logic (byte-identical to the real, UNCHANGED-by-this-slice trigger body), for the same reason: no second real 'open' service_sessions row can exist for real right now. Tests: `tests/v3NextServiceIdentity.test.js` (22 pure), `tests/serviceLifecycleV3RolloverEngine.test.js` (54 engine-integration, lettered A-N matching the session's own required list), `tests/serviceLifecycleV3RolloverMigration.static.test.js` (57 static SQL), plus `tests/serviceLifecycleV3EngineLegacyNonInterference.static.test.js` extended (63/63, now also scanning for the legacy ensure/rollover/auto-close scheduler modules and LEGACY_AUTOMATIC_LIFECYCLE_ENABLED) and `tests/supabaseResourcePolicy.test.js` extended (112/112). Does NOT open a next-next service, does NOT enable automatic scheduling, does NOT touch the 10-minute scheduler/boot-catchup/cron/LEGACY_AUTOMATIC_LIFECYCLE_ENABLED, does NOT call chiudiServizio/begin_service_session_close/storico/serata_summary — all explicitly verified absent. -->

| 63 | 2026-08-10 → **P0-B — MESA CLOSE FORCES ORPHANED ORDERS TERMINAL** | 2026-08-10_mesa_close_forces_orphaned_orders.sql | (local, not yet committed) | 2026-08-10 | dc6e1bdc898d9a967a | <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) 2026-08-10 via Supabase MCP apply_migration. Fixes: mesa_post_payment_v1 closed a table_session on financial state alone and never touched ordenes.estado, so any order still non-terminal (EN_COCINA/LISTO) when its table was fully paid became permanently unreachable — markServed() (the only estado->terminal path) is only exposed through floor()/listFloorRows(), hard-scoped to table_sessions.status='open'. Confirmed live on real pre-existing staging data (Mesa 2 table_session fa549fc2-..., orders #362/#363 stuck LISTO/EN_COCINA hours after being paid) and reproduced fresh (order #366). Not merely cosmetic: guard_service_session_closed_v1's SERVICE_ACTIVE_ORDERS_NOT_RESOLVED check (unconditional, unchanged since 2026-08-02_service_close_live_work_guard.sql through every V3 migration since) hard-blocks a SERVICE close while any such order remains non-terminal, and nothing in serviceLifecycleEngine.js/v3IncidentPolicy.js ever calls cambiaStato/changeOrderState to resolve one (grepped, confirmed absent) — an orphaned Mesa order was a ticking service-close failure, not just a stale Cocina card. FIX: CREATE OR REPLACE of mesa_post_payment_v1 only (same signature/grants), adding one block inside the existing `IF v_table_remaining_cents = 0` branch (unconditional financial-close logic entirely unchanged above it): force-transitions any of that table_session's still-non-terminal orders to CHIUSO_FORZATO (never RETIRADO — this fix cannot honestly claim the order was confirmed served, only that its table administratively closed) and writes one orden_estado_logs row per order (event_type='table_closed_forced', actor_type='system', actor_id=the paying operator, origin='mesa_table_close'), atomically in the same transaction as the table close. Scoped to table_session_id = v_session.id only; idempotent (WHERE excludes anything already terminal). Does not touch payment/financial logic, does not touch orderStateMachine.js (CHIUSO_FORZATO is not in its KNOWN_STATES, but this fix never calls cambiaStato/validateTransition at all — a pre-existing, unrelated gap, noted not fixed), does not retroactively heal orders already stranded before this migration (Mesa 2's #362/#363, this session's own test fixtures). VALIDATED prior to real apply: real BEGIN/DO/ROLLBACK behavioral probe against staging Postgres (not a mock) — confirmed order estado transitions EN_COCINA->CHIUSO_FORZATO, table_sessions.status->closed, exactly one orden_estado_logs row with correct estado_from/event_type/actor_type/origin/metadata — then rolled back, zero residue. First apply attempt's own predecessor-body guard had a bug (checked for bare 'CHIUSO_FORZATO', which already appears in this function's pre-existing billing-exclusion filters, so it always false-positived, including against the original unpatched body) — caught by the guard itself refusing the first real apply attempt (no harm, nothing was applied), fixed to check for 'table_closed_forced' instead (unique to this fix), re-applied successfully. Paired `.ROLLBACK.sql` restores the exact pre-fix body byte-for-byte (schema/behavior revert only — does not undo any orden_estado_logs row or CHIUSO_FORZATO estado this fix already wrote for real orders). Requires row 51 applied first (mesa_post_payment_v1 must exist under its mesa_* name). Does not touch service_sessions, table_sessions, guard_service_session_closed_v1, or any V3 lifecycle function. Real staging UI re-verification performed after apply — see MESA_P0_B_ORDER_TERMINALIZATION_REPORT.md for the full live E2E proof. -->

| 64 | 2026-08-10 → **P0-B.1 — DECOUPLE PAYMENT FROM TABLE/KITCHEN COMPLETION** | 2026-08-10_mesa_decouple_payment_from_close.sql | (local, this session) | 2026-08-10 | 647e731a3bd607f3 | <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) 2026-08-10 via Supabase MCP apply_migration (applied by the interrupted prior P0-B.1 session; confirmed still live by this recovery session via a direct pg_proc probe — mesa_post_payment_v1's real body carries this migration's own inline marker comment and no longer contains 'table_closed_forced'; mesa_close_session_v1 exists, single overload, service_role-only EXECUTE, anon/authenticated denied — before this recovery session wrote a single byte of SQL). PRODUCT CORRECTION to row 63: PAID != SERVED, PAID != ORDER TERMINAL, PAID != TABLE CLOSED are three independent domains; row 63 solved a real bug (orphaned non-terminal orders becoming unreachable after a financial close) for the WRONG trigger, because at the time "table fully paid" and "table administratively closed" were the same event inside mesa_post_payment_v1 (it auto-closed table_sessions on zero balance since long before row 63, and row 63 added CHIUSO_FORZATO force-termination as a side effect of that same implicit close). ROOT CAUSE: mesa_release_empty_session_v1 hard-refuses (MESA_TABLE_HAS_ORDERS) the instant covers_total is set, so full payment was structurally the ONLY event that could ever close an occupied table_session — TabMesa.jsx's own "Cerrar mesa" button was, and remains after this migration on its own, gated on `coversTotal == null` for exactly that reason. FIX, two parts in one migration: PART 1 — CREATE OR REPLACE of mesa_post_payment_v1 (same signature/grants) removes BOTH the implicit table_sessions close-on-zero-balance UPDATE (pre-dates row 63) AND row 63's CHIUSO_FORZATO block; the function now only ever writes payment_transactions/payment_allocations/order_financial_events and the legacy cobrado/ya_pagado/metodo_pago projection, never table_sessions or ordenes.estado; `tableStatus` in its response is corrected from a financial guess to the literal, structurally-guaranteed 'open' (grepped: unused anywhere in the frontend). PART 2 — new `mesa_close_session_v1(p_workspace_id, p_by_actor, p_table_session_id, p_force default false)`, the first real explicit "Cerrar mesa" for an OCCUPIED table (mesa_release_empty_session_v1 untouched, stays the correct path for the never-ordered case): outstanding>0 always raises MESA_TABLE_NOT_SETTLED, force or not (financial safety is never overridden by p_force); with outstanding=0, non-terminal orders block close by default (MESA_TABLE_HAS_ACTIVE_ORDERS) unless p_force=true, in which case they force-terminalize to CHIUSO_FORZATO (row 63's logic, moved here verbatim, same honest semantics, same audited orden_estado_logs trail — but now actor_type='operator', not 'system', because this is a genuine explicit human decision, not an implicit payment side-effect); idempotent-safe the same way mesa_release_empty_session_v1 already is (a retry after success fails MESA_SESSION_NOT_OPEN before any mutation). REVOKE ALL/GRANT EXECUTE TO service_role only, same as every other mesa_*_v1 RPC. Guarded against double-apply: refuses unless mesa_post_payment_v1 still carries row 63's body (`table_closed_forced` marker) AND mesa_close_session_v1 does not already exist. Paired `.ROLLBACK.sql` restores mesa_post_payment_v1 code-identical to row 63's body (comments necessarily reworded — "see this file's header" would dangle in a different file — verified by a comment-stripped equality test, not literal byte-identity) and drops mesa_close_session_v1 (the only migration that ever created it); does not touch mesa_release_empty_session_v1. Requires row 63 applied first (guard-checked). Does not touch service_sessions, guard_service_session_closed_v1, or any V3 lifecycle function — Lifecycle V3 is unmodified and continues to correctly observe PAID+OPEN+EN_COCINA as true pending operational work, not a false incident. VALIDATED: 45/45 static structural tests (tests/mesaDecouplePaymentFromClose.static.test.js) — predecessor-body guard, idempotent-safe guard, no destructive DDL, grants scoped correctly, PART 1 signature/financial-logic byte-for-byte (comments aside) identity with row 63 up to the removed close block, PART 2 financial-safety-before-force-branch ordering, force-only kitchen gate, CHIUSO_FORZATO-never-RETIRADO honesty, FOR UPDATE locking, role-set parity with releaseEmptyTable, rollback code-identity. JS-SIDE (this recovery session, backend code was NOT yet wired when this migration was first applied): src/tables/mesaDao.js (`closeSession`), src/tables/mesaService.js (`closeTable`, reuses OPEN_ROLES — same operational-floor role set as releaseEmptyTable), src/tables/mesaHttpHandlers.js (`POST /sessions/:sessionId/close`, safeError conflict/denied Set entries for MESA_TABLE_NOT_SETTLED/MESA_TABLE_HAS_ACTIVE_ORDERS/MESA_CLOSE_FORBIDDEN, route count 11→12), src/utils/supabaseResourcePolicy.js (rpc/mesa_close_session_v1, INTERNAL_OPERATIONAL). force is accepted end-to-end at every backend layer but the frontend does not call it with force=true anywhere — no force-close UI/second-confirmation flow was built (explicitly out of scope per the session brief; backend-only support is intentional). FRONTEND (ladieci-messa-staging-frontend, ladieci-app33/src/components/mesa/TabMesa.jsx + src/mesa/mesaApi.js): "Cerrar mesa" button visibility changed from `coversTotal == null` to also allow occupied tables; confirmClose now calls the new close endpoint for occupied tables and the pre-existing release endpoint for the never-ordered case, so the same button and confirmation dialog serve both without a redesign; a blocked close (unpaid balance or genuine pending kitchen work) surfaces via the dialog's existing error banner using two new Spanish error-message entries. See MESA_P0_B_1_RECOVERY_REPORT.md for the full recovery narrative, real-Postgres reconciliation proof, and staging E2E. -->
| 65 | 2026-08-10 → **P0-C2 — AUTHORITATIVE INTRADAY ECONOMIC BOUNDARY** | 2026-08-10_service_lifecycle_economic_boundary_v1.sql | applied | 2026-08-10 | f76543f7f1054ccc | <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) 2026-08-10 via Supabase MCP apply_migration, immediately after a 16/16-check validation against an isolated synthetic shadow schema (p0c2_shadow, zero references to real rows, fully discarded — no COMMIT — before this real apply). Purely additive: one new service_sessions.status value ('rolled_over'), one new column (rolled_over_at timestamptz), one new RPC (roll_service_session_economic_v1). Root cause audited first: guard_service_session_closed_v1's SERVICE_ACTIVE_ORDERS_NOT_RESOLVED check fires on ANY transition into status='closed' unconditionally — confirmed still true against the LIVE function body (not just the migration file), unchanged through Slice 3.3 by that migration's own header ("does NOT modify close_service_session_v3 or guard_service_session_closed_v1 at all"). An ordinary intraday PRANZO->SERA boundary must coexist with real non-terminal orders (P0-C2's clarified product contract), so routing it through status='closed' would hit that hard block for real. 'rolled_over' sidesteps guard_service_session_closed_v1 entirely (it only ever inspects NEW.status='closed') — zero risk to any existing V2 or V3 close path, no new exemption branch added to that trigger. closed_at is deliberately never reused for this status (service_sessions_check enforces status='closed' <=> closed_at IS NOT NULL bidirectionally); rolled_over_at is its own honest column. roll_service_session_economic_v1(p_service_session_id, p_closeout_correlation_id, p_actor, p_source, p_next_service_kind, p_next_business_date): one atomic transaction — same advisory-lock namespace ('service_session_lifecycle') as close_service_session_v3/ensure_service_session, so can never race either — that (1) requires a service_closeouts row to already exist for the exact (session, correlation) pair (same precondition style as close_service_session_v3; create_service_closeout, row 58/61, is the only writer, unmodified), (2) transitions A directly from 'open' OR 'closing' to 'rolled_over' (accepting 'closing' specifically so this can also reconcile a session stuck mid legacy-close, e.g. the 2026-08-10 15:37 UTC stuck PRANZO — see P0_C2 report §20), (3) INSERTs B fresh/open/current in the SAME transaction (same column shape as ensure_service_session's own INSERT), (4) flips service_session_state.current_session_id atomically — no window where order attribution is ambiguous, since ordenes_assign_service_session's own hard guard (INVALID_OPEN_SERVICE_SESSION) requires the pointed-to session to be status='open' at INSERT time. NEVER touches ordenes or table_sessions (zero DML against either table anywhere in the function body) — A's non-terminal orders/open tables remain exactly as they are, permanently and correctly attributed to A. Idempotent: a retry with the same (session, correlation) after a prior success returns ALREADY_ROLLED_OVER with the same B (via the rollover_source_session_id unique index), never a duplicate. VALIDATED (see migration file header + P0_C2 report §14): 16/16 checks in an isolated shadow schema — atomic transition, A's closed_at stays null, A's orders byte-identical before/after (md5 hash compared), zero orders deleted, a new order created after rollover correctly attributes to B and never to A, idempotent retry returns the same B with no duplicate, single-active-session invariant holds throughout. Deliberately does NOT touch LEGACY_AUTOMATIC_LIFECYCLE_ENABLED, does not wire any scheduler/cron/timer, does not modify chiudiServizio/incidentSafeRollover/ensureServiceSession/close_service_session_v3/guard_service_session_closed_v1/service_sessions_single_active_uq. JS-SIDE: src/schedule/serviceSchedule.js (new resolveEconomicPeriod, a single deterministic no-buffer cutoff, distinct from resolveSchedule's operational ensure-windows), src/serviceSessions/economicBoundaryEngine.js (new orchestrator: reuses closeoutAttempts/closeoutSnapshots/serviceCloseoutCreation/currentServiceCloseout.aggregate() unmodified, classifies zero "incidents" for ordinary non-terminal-order/open-table carryover per the clarified product contract, keeps the one true integrity check — paid-vs-ledger reconciliation mismatch — as a hard blocker). See P0_C2_INTRADAY_ECONOMIC_BOUNDARY_REPORT.md for the full design rationale, live schema audit, and staging E2E. -->
| 66 | 2026-08-11 → **P0-C2 HOTFIX — service_session_audit event_type widen** | 2026-08-11_service_lifecycle_economic_boundary_v1_audit_fix.sql | applied | 2026-08-11 | c175f8d19c191a7c | <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) 2026-08-11 via Supabase MCP apply_migration, immediately after a 4/4-check validation against an isolated shadow copy of the real service_session_audit_event_type_check constraint (p0c2_audit_fix_shadow, discarded — no COMMIT — before this real apply). Fixes a real gap in row 65: roll_service_session_economic_v1 writes a service_session_audit row with event_type='rolled_over_economic' for session A, but the pre-existing service_session_audit_event_type_check constraint (predates row 65, untouched by it) only allowed ('opened','closing','closed') — never widened when row 65 introduced the new event type. Surfaced by the FIRST real invocation of roll_service_session_economic_v1 (the controlled-recovery attempt against the actual stuck 2026-08-10 PRANZO, correlation 76c44f7d-...): failed closed with a Postgres CHECK violation inside the function body, which rolled back the entire transaction atomically (session A's status update, B's creation, and the current_session_id pointer flip all rolled back together — confirmed via direct query immediately after: session A still status='closing', rolled_over_at NULL, no B row existed for 2026-08-10). No partial mutation occurred — this was a pure schema gap, not a logic bug in the rollover itself, not a data-integrity incident. Not caught by row 65's own shadow-schema validation or this slice's unit tests because neither ever sent a real INSERT through the actual live constraint (unit tests mock the `rpc` dependency entirely; the real-Postgres validation exercised the RPC's own controlled jsonb-error paths, not a full run to the audit INSERT). Purely additive, single constraint, nothing else touched: DROP+ADD CONSTRAINT service_session_audit_event_type_check widened from 3 to 4 allowed values. Guarded against double-apply and against running before row 65 or against a drifted pre-existing definition (RAISE EXCEPTION on all three). VALIDATED: 4/4 shadow-schema checks (pre-fix insert of 'rolled_over_economic' correctly rejected — proving the shadow faithfully reproduced the real bug; post-fix insert of 'rolled_over_economic' succeeds; all 3 original values still accepted; an arbitrary bogus value is still rejected — the constraint stays meaningfully restrictive, not opened wide) plus 17/17 static structural tests (tests/serviceLifecycleEconomicBoundaryAuditFix.test.js). Paired `.ROLLBACK.sql` restores the original 3-value CHECK, refuses if any real 'rolled_over_economic' row exists. See P0_C2_INTRADAY_ECONOMIC_BOUNDARY_REPORT.md for the full incident narrative and recovery retry proof. -->
| 67 | 2026-08-12 → **SERVICE LIFECYCLE RUNTIME AUTHORITY RECOVERY — begin_service_session_close stale-return fix** | 2026-08-12_service_session_close_stale_return_fix.sql | 4a439b5 | 2026-08-12 | 1ede59519bf77160 | <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) 2026-08-12 via Supabase MCP apply_migration, verified live via pg_get_functiondef immediately after. Fixes a correctness bug in begin_service_session_close that predates this session's work: on the open→closing path it captures v_session via SELECT...INTO, then UPDATEs that same row's status/closed_by/close_source/updated_at — but the final RETURN serialized the PRE-update in-memory snapshot, because v_session itself was never reassigned after the UPDATE, so every caller saw status still 'open' in the returned payload even though the row was genuinely 'closing' in the database. src/utils/servizio.js's close engine treats that stale 'open' as a hard identity-mismatch immediately after this call and aborts the entire close, leaving the session stuck in 'closing' forever. Never triggered before because no automatic path had attempted a fresh open→closing transition through this exact call site in a long time; surfaced by the FIRST real invocation during this session's stale-2026-08-11-PRANZO recovery (session 421f93e1-ecd5-4da7-b82b-b89d75ecd3a7). ONE line changed: the UPDATE gains `RETURNING * INTO v_session`, replacing the stale pre-UPDATE snapshot with the row this statement itself just wrote. Same signature, same guards, same advisory lock, same audit-row insert. Idempotent (CREATE OR REPLACE). Paired `.ROLLBACK.sql` restores the pre-fix body. See SERVICE_LIFECYCLE_RUNTIME_AUTHORITY_RECOVERY_REPORT.md for the full incident narrative. -->
| 68 | 2026-08-13 → **NATURAL ROLLOVER FINAL PROOF — guard_service_session_closed_v1 incident-safe exemption** | 2026-08-13_guard_service_session_closed_incident_safe_exemption.sql | (local, not yet committed) | 2026-08-13 | 8789a5623bd98a14 |
| 69 | 2026-08-14 → **MESA SEND-TO-KITCHEN P0 FIX — covers authoritative at selection time (mesa_set_session_covers_v1)** | 2026-08-14_mesa_covers_authoritative_on_selection.sql | (local, not yet committed) | 2026-08-14 | 27bdc06d366b73db | <!-- ROOT CAUSE (proven live on staging 2026-08-14): V3-I (row 50) deliberately deferred table_sessions.covers_total to be set ONLY atomically inside the first comanda's own INSERT, to avoid stranding a confirmed guest count against a failed comanda. Side effect V3-I did not anticipate: for the entire time an operator is actively building a draft (can be many minutes), covers_total stays NULL -- the ONLY signal mesa_release_empty_session_auto_v1 (row 60) has to tell a genuinely empty table apart from a real order in progress. The V3 service-lifecycle close engine (serviceLifecycleEngine.js) calls that RPC as a safe auto-action on every service close/rollover, for every open table session with covers_total IS NULL. Live proof: service_incidents row 8c72c457-c092-4e37-a88f-a624fe3bb0dc, detected_at 2026-08-14 19:38:47.468639+00, incident_type EMPTY_TABLE_LEFT_OPEN, entity_id 039d2644-4068-4902-9fa0-6ed68c776e33 (Mesa 1 table_session open 19:08:53 -> 19:38:47, ~30 minutes), resolution_type auto_released_empty_table, resolution_note "Table session had zero activity (covers_total IS NULL) at service close; automatically released." table_sessions.closed_at for that row matches to the millisecond -- a routine service close silently destroyed a real, fully-built 113,50 EUR draft. FIX: purely additive, one new RPC (mesa_set_session_covers_v1), zero changes to any existing function/trigger/table/column. Lets the backend persist covers_total the moment the operator selects them (MesaOrderBuilder's covers step, before the picker even opens) -- immediately protected by the ALREADY-EXISTING monotonic guard trigger (mesa_guard_covers_monotonic_v1, row 50, unchanged) and the ALREADY-EXISTING auto-release guard (mesa_release_empty_session_auto_v1, row 60, unchanged -- `covers_total IS NOT NULL` now correctly reads true the instant covers are chosen). mesa_prepare_table_order_v1 (row 50/61) needs no change: its own existing branch already treats "covers already set" as a documented no-op. V3-I's original concern (covers registered but the comanda then fails) is deliberately accepted now as a PRODUCT decision: a table with covers set but zero orders must require an EXPLICIT close, never a silent auto-release -- see MESA_SEND_TO_KITCHEN_P0_FIX_2026-08-14.md, invariant D. 13/13 static structural tests (tests/mesaCoversAuthoritativeOnSelection.static.test.js). Paired `.ROLLBACK.sql` drops the new function only. -->| <!-- APPLIED ON STAGING (tdikhfeinufaahagmpjz) 2026-08-13 via Supabase MCP apply_migration, verified live via pg_get_functiondef + pg_proc overload check immediately after. Fixes a gap found live while proving the row-67 fix: preserveActiveOrders (commit 4a439b5) correctly stops the close engine from archiving a residual non-terminal order, but the independent guard_service_session_closed_v1 trigger unconditionally re-checks for non-terminal orders on every transition to status='closed' and raises SERVICE_ACTIVE_ORDERS_NOT_RESOLVED regardless — proven live: session 84d9e4f4 (2026-08-12 SERA) got stuck in status='closing', order #369 fully intact (not archived, not deleted — that part of the fix held), but the close itself could not finish. The guard's existing exemption clause checks a marker (ladieci.v3_close_authorized_session_id) that only close_service_session_v3 ever sets, and close_service_session_v3/serviceLifecycleEngine.js is never require()'d by index.js or anything reachable from it (confirmed by repo-wide grep) — the exemption was orphaned/unreachable from the real runtime, and always had been (a prior P0-C2 session, row 65, independently hit the same wall and worked around it with an entirely separate mechanism rather than touch this guard — see economicBoundaryEngine.js's own header). FIX: complete_service_session_close gains p_preserve_active_orders (DEFAULT false); when true it sets a new, actually-reachable marker (ladieci.incident_safe_close_session_id) before its UPDATE. guard_service_session_closed_v1 gains one additive exemption keyed to that marker: table check tolerates an open table_session when the marker matches (same unconditional-for-tables semantics allowOpenTablesAcrossBoundary already has at the JS layer — this migration is what finally makes that reachable at the DB layer too, fixing the identical latent gap allowOpenTablesAcrossBoundary always had); order check tolerates a non-terminal order ONLY when the marker matches AND that exact order already has its own service_incidents row (order_id match) — enforced per-order, not a blanket bypass, proving "canonical and incident-backed" at the DB level rather than trusting the caller's claim. Every current_setting(...) comparison against the new marker is COALESCE-wrapped to a clean boolean (never a bare comparison that could evaluate to NULL and silently skip a check for every caller — the pre-existing v3 clause happens to be safe today only because it is AND-ed with service_closeouts EXISTS(), which is always false on the real V2 runtime path; the new marker is OR-ed, not AND-ed, into the table check, so it cannot rely on that same coincidence). Only incidentSafeRollover.js's one call site ever sets preserveActiveOrders=true, so only it can ever set the new marker — the manual force-close HTTP action and the frozen legacy automatic path are unaffected, guard remains exactly as strict for both. LIVE OVERLOAD BUG CAUGHT AND FIXED DURING THIS SAME APPLY: the first attempt used CREATE OR REPLACE for complete_service_session_close's widened signature (3 args -> 4); Postgres treats a changed parameter-type list as a NEW overload regardless of defaults, so both the old 3-arg and new 4-arg functions existed simultaneously after that first apply (confirmed via pg_proc). Corrected live with an explicit DROP FUNCTION on the 3-arg overload (the only real caller, serviceSessionLifecycle.js, always sends all 4 named params), then the migration file itself was rewritten to DROP the old overload before CREATE (not CREATE OR REPLACE) so a fresh apply from the file alone produces the same single-overload result — matching this codebase's own established discipline for a widened signature (see row 61). guard_service_session_closed_v1 keeps CREATE OR REPLACE (its own signature never changes — a trigger function takes no arguments). No table/column/data touched. 18/18 static structural tests (tests/guardServiceSessionClosedIncidentSafeExemption.static.test.js), including 3 added specifically to pin the DROP-before-CREATE discipline after the live overload bug. Paired `.ROLLBACK.sql` drops the 4-arg overload and restores the original 3-arg complete_service_session_close plus guard_service_session_closed_v1's pre-fix body, verbatim; refuses if drifted. See SERVICE_LIFECYCLE_RUNTIME_AUTHORITY_RECOVERY_REPORT.md for the full incident narrative and recovery retry proof. -->
| 70 | 2026-08-15 → **STALE_SERVICE_SESSION_SELF_HEAL — begin_service_session_close incident-safe exemption (third and final gate)** | 2026-08-15_begin_service_session_close_incident_safe_exemption.sql | 183ed7d (allowActiveRiderTripAcrossBoundary, preceding this migration) | 2026-08-15 | c5103d5bfd438ad5 | <!-- ROOT CAUSE (proven live on staging 2026-08-15, against the real stuck 2026-08-13 SERA session c9d5aaa7-d0d5-4740-a6ee-83a8ee57adda): three INDEPENDENT places check "are there open Mesa tables blocking this close" -- (1) chiudiServizio's own JS-level gate, exempted via closeContext.allowOpenTablesAcrossBoundary since Slice 4C.1; (2) guard_service_session_closed_v1's trigger check on the closing->closed transition, exempted since row 68; (3) THIS function, begin_service_session_close, on the open->closing transition, which had never been given any exemption at all. Proven live in sequence: this session's own separate allowActiveRiderTripAcrossBoundary fix (backend commit 183ed7d) correctly let a real incident-safe rollover attempt past the previously-blocking rider-trip gate for the first time -- and the very next step failed here instead, with the identical MESA_TABLES_NOT_RELEASED code gates #1/#2 already tolerate, proving gate #3 was the one nobody had connected to the other two. FIX: begin_service_session_close gains p_preserve_active_orders (DEFAULT false) -- same name, same meaning, same default as complete_service_session_close already has (row 68); when true the MESA_TABLES_NOT_RELEASED check is skipped, tolerating an open table_session exactly like gates #1/#2 already do. No table_session row is ever read for any purpose beyond the existence check, never written, never closed, never reassigned -- an open table crossing the boundary stays open and unchanged. Only incidentSafeRollover.js's own call site (via chiudiServizio -> serviceSessionLifecycle.beginClose) can ever set it true; the manual HTTP close action and the frozen legacy automatic path never pass it, keeping today's strict default behavior for both. Predecessor-body guard (DO block) refuses to apply over drift, an unexpected body, or a re-patch. Signature widens 2 args -> 3 -- DROP before CREATE (not bare CREATE OR REPLACE), matching this codebase's own established discipline for a widened signature (rows 61, 68). 26/26 static structural tests (tests/beginServiceSessionCloseIncidentSafeExemption.static.test.js) plus JS-level forwarding tests (tests/serviceSessionLifecycle.test.js, tests/serviceCloseGate.test.js) proving both halves of the close transition (begin + complete) now express one identical, non-contradictory preservation policy. Paired `.ROLLBACK.sql` drops the 3-arg overload and restores the original 2-arg begin_service_session_close (row 67's exact post-stale-return-fix body), verbatim. See STALE_SERVICE_SESSION_SELF_HEAL_ROOT_CAUSE_2026-08-15.md for the full incident narrative and recovery retry proof. -->
| 71 | 2026-08-15 → **MESA REMEDIATION S1 — guard_service_session_closed_v1 NULL fix + payment idempotency narrowing + duplicate-candidate window** | 2026-08-15_s1_guard_null_payment_idempotency.sql | (local, not yet committed) | 2026-08-15 | b56385d2648dd427 | <!-- Authority: MESA_REMEDIATION_PLAN_FINAL_V2_1_2_2026-08-15.md, slice S1. NOTE: payment_transactions_idempotency_uq turned out live to be a genuine UNIQUE table CONSTRAINT (contype='u'), not a bare index -- DROP INDEX on it fails with 2BP01; constraint-level DDL (ADD/DROP/RENAME CONSTRAINT) used throughout instead, same new-before-old ordering. Three independent fixes in one migration. (A) guard_service_session_closed_v1's table-open check read current_setting('ladieci.v3_close_authorized_session_id', true) = OLD.id::text unwrapped -- when unset (the ordinary case for every close path except the unreachable legacy V3 engine), current_setting returns NULL, and NULL propagates through the surrounding AND/OR/NOT to make the whole clause NULL, which PL/pgSQL's IF treats exactly like false: the open-table EXISTS check and its MESA_TABLES_NOT_RELEASED raise were silently skipped whenever a service_closeouts row already existed for the session (true today for any session an economic-boundary or V3-style engine has touched -- proven live: 33174121 and 9746dfdd both have one), exactly the class of defect row 68's own header warned a different marker could hit ("safe today only because it is AND-ed with service_closeouts EXISTS(), which is always false on the real V2 runtime path" -- economicBoundaryEngine.js has since made that false). FIX: the same COALESCE(current_setting(...,true),'')=OLD.id::text discipline row 68 already established for v_incident_safe is applied to this clause too, via a new v_v3_authorized local. No new lifecycle exception, no new allow...AcrossBoundary flag. (B) payment_transactions_idempotency_uq narrowed from (workspace_id, by_actor, by_sid_hash, client_request_id) to (workspace_id, client_request_id) -- F-15: a re-login or second device previously defeated replay protection for the same logical payment. New index created before the old one is dropped (uniqueness never absent for any interval), inside an explicit LOCK TABLE ... SHARE ROW EXCLUSIVE MODE. mesa_post_payment_v1's replay lookup narrows to match; a request_hash mismatch on the same key still raises MESA_PAYMENT_IDEMPOTENCY_CONFLICT (23505); a replay by a different actor still returns idempotent:true (zero new rows) but now also appends an auth_audit row (event PAYMENT_REPLAY_DIFFERENT_ACTOR) naming both identities -- never silent, never blocked. (C) new duplicate-candidate window (NOT idempotency): before INSERT, the same table_session_id is searched for a payment matching kind/mode/amount/payment_method/covers_settled under a DIFFERENT client_request_id created within the previous 120 seconds; a hit raises MESA_POSSIBLE_DUPLICATE_PAYMENT unless the caller passes the new p_confirm_duplicate := true, in which case the payment proceeds and an auth_audit row (event PAYMENT_DUPLICATE_CONFIRMED) records the override -- never returned as idempotent:true. Signature widens 12 args -> 13 (p_confirm_duplicate appended, DEFAULT false) -- DROP before CREATE, matching this codebase's established discipline (rows 61, 68, 70); guard_service_session_closed_v1 keeps CREATE OR REPLACE (trigger function, no argument list). Root cause discovered live while executing the Mesa remediation's S0 baseline (see MESA_REMEDIATION_PLAN_FINAL_V2_1_2_2026-08-15.md §0.1 for the sibling closeout-counter defect found in the same investigation, owned by a later slice, S18 -- unrelated to this migration). Nothing else changes: no table/column added or dropped, no historical payment_transactions row touched, none of S2's objects (receipt-service nullability, event_service_session_id, mesa_snapshot_order_lines_v1, service_session_assign_financial_event, order_entities, order_uid, the S8 bypass tables, balance semantics, adjustment tables) referenced. 44/44 static structural tests (tests/s1GuardNullPaymentIdempotency.static.test.js). Paired `.ROLLBACK.sql` restores both function bodies verbatim and the original 4-column index; refuses if either function has drifted or already carries this migration's changes. -->
| 72 | 2026-08-15 → **MESA REMEDIATION S1 FOLLOW-UP — auth_audit.event CHECK widening for the two row-71 payment-audit events** | 2026-08-15_s1_auth_audit_payment_events.sql | (local, not yet committed) | 2026-08-15 | 7d05b06fa3ca2f30 | <!-- Authority: MESA_REMEDIATION_PLAN_FINAL_V2_1_2_2026-08-15.md, slice S1. Row 71 was applied to STAGING before this gap was found live: mesa_post_payment_v1's two new auth_audit INSERT paths (event PAYMENT_REPLAY_DIFFERENT_ACTOR on a replay-by-different-actor, event PAYMENT_DUPLICATE_CONFIRMED on a p_confirm_duplicate override) both used event literals that were never added to auth_audit_event_chk -- either path aborted the WHOLE payment on a CHECK violation instead of merely auditing it, discovered during row 71's own live validation. FIX: same widen-never-narrow discipline as every prior auth_audit_event_chk migration (2026-07-13_auth_active_events, 2026-07-15_auth_unlocked_event, 2026-07-29_v3a_access_control_foundation) -- DROP + ADD CONSTRAINT, preserving all 22 currently allowed events verbatim, adding exactly these two. Deliberately does NOT edit row 71's already-applied migration file (would misrepresent applied history and desync its recorded checksum) and does NOT touch row 71's own DB objects (guard_service_session_closed_v1, payment_transactions_idempotency_uq, mesa_post_payment_v1) -- purely the CHECK-constraint gap, closed as its own migration. Also deliberately does NOT widen src/auth/audit.js's Node-side ALLOWED_EVENTS: mesa_post_payment_v1 writes auth_audit directly via SQL inside the Postgres function, never through Node's assertEvent, so ALLOWED_EVENTS has zero bearing on this fix, and widening it would force an edit to tests/authDao.test.js's unrelated hardcoded length===8+3+11 assertion for a different subsystem. Casing (UPPER_SNAKE_CASE vs. every pre-existing lower_snake_case event) reproduces literally what row 71's already-applied function body inserts -- not a new convention chosen here. 28/28 static structural tests (tests/s1AuthAuditPaymentEventsMigration.static.test.js). Paired `.ROLLBACK.sql` narrows the CHECK back to the pre-existing 22-event set; refuses if any audit row already uses either new event. -->
| 73 | 2026-08-15 → **MESA REMEDIATION S2 — three attribution writers + receipt-service schema** | 2026-08-15_s2_attribution_writers_receipt_service.sql | (local, not yet committed) | 2026-08-15 | ad5e45e02c45cdf8 | <!-- Authority: MESA_REMEDIATION_PLAN_FINAL_V2_1_2_2026-08-15.md, slice S2 (§5 BLOCKER A). One migration transaction, DDL + all three function replacements, no DROP FUNCTION anywhere (all three signatures unchanged from S1 -- plain CREATE OR REPLACE). (A) mesa_snapshot_order_lines_v1 (F-02): table_order_lines.service_session_id was stamped from v_session.service_session_id (the TABLE's historical origin service, looked up from table_sessions) even though V3.1 already made ordenes_assign_service_session the sole writer of order->service attribution and NEW.service_session_id on the ordenes row is already correct by the time this AFTER INSERT trigger fires -- an order placed on a table after it crossed a service boundary got its lines silently mis-attributed to the stale origin service. Fix is a one-column swap: NEW.service_session_id instead of v_session.service_session_id. Proven live pre-fix: F-02, 4 orders / EUR 47.51 (historical, NOT repaired -- S2 fixes future writers only). (B) service_session_assign_financial_event: replaced the two-line pre-S2 body (RAISE ORDER_WITHOUT_SERVICE_SESSION whenever the order row is gone or de-linked -- exactly the EUR 140.50/11-line S0 §A population) with the plan's own §5.3 CORRECTION 1 body verbatim: STEP 1 unchanged (live order, exact); STEP 2 fires ONLY when the order is already gone AND a payment_transaction_id exists, resolving the transaction's own immutable table_session_id then joining payment_allocations to table_order_lines by table_order_line_id (uuid, never recycled), never by display id; more than one distinct candidate raises ORDER_OBLIGATION_AMBIGUOUS, never guesses; zero evidence falls through to STEP 3's unchanged fail-closed RAISE. The forbidden V2.1 fallback (storico ORDER BY id DESC LIMIT 1 -- "newest wins") does not appear anywhere in this file. (C) mesa_post_payment_v1 (F-29 + event service + cobrado-projection consistency): payment_transactions.service_session_id was likewise stamped from v_session.service_session_id (table origin) -- cash received after a boundary was booked to a possibly-closed session. Fix: a new local v_receipt_service_id resolved from service_session_state (the same singleton pointer service_session_assign_order already trusts), requiring status='open'; a miss leaves it NULL with no RAISE -- off-service receipts are legitimate, unlike order creation. Used for BOTH payment_transactions.service_session_id (receipt) and the new order_financial_events.event_service_session_id (event) -- same real-world instant, same value. order_financial_events.service_session_id keeps its OBLIGATION meaning and is never written by the RPC (the BEFORE INSERT trigger from part B overwrites it regardless). Proven live pre-fix: F-29, 2 transactions / EUR 77.51 (historical, NOT repaired). Incidentally found and fixed in the same pass: three correlated subqueries (the per-order paid-before lookup, and two inside the final cobrado/ya_pagado projection) filtered order_financial_events by e.service_session_id = v_session.service_session_id (table origin) -- once table_order_lines/order_financial_events both correctly mean "this order's own service" (part A + B), that filter silently found zero events for any cross-boundary order, always computing paid-before = 0. Fixed by joining ordenes for the mirror-loop's own obligation service, and filtering the projection on l.service_session_id (with l.service_session_id added to its GROUP BY -- functionally dependent on l.order_id, required by Postgres for a column referenced inside a correlated subquery in a grouped query's SELECT list). For a non-crossing order both fixes produce byte-identical output to before; only crossing orders' output changes, and only to become correct. (D) schema: payment_transactions.service_session_id DROP NOT NULL (strictly widening, cannot fail on existing rows); order_financial_events ADD COLUMN event_service_session_id uuid NULL REFERENCES service_sessions(id) (unindexed -- order_financial_events' six partial unique indexes are untouched, no row can change namespace since no existing service_session_id value ever changes). No backfill: every existing order_financial_events row keeps event_service_session_id NULL, reported as legacy/unknown, per V2.1.2's explicit HISTORICAL METHOD: none. No UPDATE/DELETE against table_order_lines, payment_transactions, payment_allocations, or order_financial_events anywhere in this file; all four tables' append-only triggers untouched and unbypassed. S1's idempotency key, NULL guard fix, and duplicate-candidate window are reproduced verbatim (unavoidable -- CREATE OR REPLACE needs the full body) and otherwise untouched. 39/39 static structural tests (tests/s2AttributionWritersReceiptService.static.test.js). Paired `.ROLLBACK.sql` restores all three function bodies verbatim, drops event_service_session_id, and restores NOT NULL -- refuses if any payment_transactions row has service_session_id IS NULL (a real off-service receipt committed under S2; the rollback stops and escalates rather than deleting real money). -->
| 74 | 2026-08-15 → **MESA REMEDIATION S4 — public.ladieci_schema_migrations ledger (table + immutability trigger + REVOKE/RLS)** | 2026-08-15_s4_ladieci_schema_migrations_ledger.sql | (local, not yet committed) | 2026-08-15 | e663ccbaec2fb89f | <!-- Authority: MESA_REMEDIATION_PLAN_FINAL_V2_1_2_2026-08-15.md, slice S4 (§15). Creates the project-owned migration-authority ledger, distinct from the Supabase-owned supabase_migrations.schema_migrations (which exists live, 67 rows, and is never written by this project). Columns/constraints per §15 verbatim: verification_status IN ('verified','bootstrapped_unverified'), ladieci_schema_migrations_verified_chk (verified requires verified_at+verification_method; bootstrapped_unverified requires verified_at IS NULL), apply_order UNIQUE, checksum_sha256 shaped to this project's existing 16-hex-char manifest convention. REVOKE ALL FROM anon,authenticated + RLS enabled with zero policies -- service_role only. Immutability trigger (BEFORE UPDATE OR DELETE at this point in the slice -- see row 76 for the INSERT-time follow-up) permits exactly one UPDATE transition (bootstrapped_unverified -> verified, immutable facts unchanged) and unconditionally rejects DELETE. Predecessor guard also escalates if a bare public.schema_migrations existed (S4's own named collision-avoidance clause) or if any S5+ table exists. APPLIED ON STAGING (tdikhfeinufaahagmpjz) 2026-08-15 via Supabase MCP apply_migration; post-condition DO block asserts the table/trigger/RLS all exist before returning success. Paired `.ROLLBACK.sql` drops trigger+function+table (cascades away all seeded rows -- the only possible way to remove rows given DELETE is unconditionally forbidden), refusing if any row carries real post-bootstrap verification work. -->
| 75 | 2026-08-15 → **MESA REMEDIATION S4 — bootstrap seed (73 rows: 38 verified / 35 bootstrapped_unverified)** | 2026-08-15_s4_ladieci_schema_migrations_bootstrap_seed.sql | (local, not yet committed) | 2026-08-15 | 85f04fecf8d1a9e1 | <!-- Authority: MESA_REMEDIATION_PLAN_FINAL_V2_1_2_2026-08-15.md, slice S4 (§15 HISTORICAL METHOD: "one-off seeding from the manifest using the S0 §K classification. Nothing is asserted that was not proven."). Source: ops/baseline/mesa_baseline_2026-08-15.json section_K (rows 1-70, frozen at S0) plus rows 71-73 (created after S0 by this same remediation's S1/S2, classified by applying the identical mechanical method -- exact filename-slug match against supabase_migrations.schema_migrations.name -- re-verified live in S4's own Phase 0/1, not merely assumed). All 73 rows kind='bootstrap' (pre-epoch by construction, per §15's REQUIRED_MIGRATIONS rule of only counting kind<>'bootstrap'). TWO DATA-QUALITY GAPS found and corrected while building the seed, both flagged in the affected rows' own notes column, neither touching any migration FILE's content nor reclassifying any row's verification_status: (1) checksum_sha256 for 5 rows (apply_order 51,52,53,55,63) -- the S0 §K JSON held stale placeholder text or a malformed-length value; recomputed first-party via sha256 of the exact currently-committed file. (2) applied_by for 21 rows -- the manifest's own "commit" column was never retroactively filled in and still held authoring-time placeholders ('pending','applied','(local, not yet committed)', etc); resolved first-party via `git log --diff-filter=A` against the actual committed history, rather than seed literal "pending" into a ledger whose own GOAL is "semantically truthful from day one". IDEMPOTENCY: INSERT...ON CONFLICT(filename) DO NOTHING. Post-condition DO block asserts exactly 73/38/35/max(apply_order)=73 before returning success -- independently re-verified live post-apply (matches). Paired `.ROLLBACK.sql` is documentation-only: rows cannot be deleted in isolation under the append-only trigger, so rolling back the seed means rolling back the whole ledger via row 74's own ROLLBACK.sql. -->
| 76 | 2026-08-15 → **MESA REMEDIATION S4 FOLLOW-UP — INSERT-time checksum-conflict guard (fail-closed on same-filename/different-checksum)** | 2026-08-15_s4_ladieci_schema_migrations_insert_checksum_guard.sql | (local, not yet committed) | 2026-08-15 | 2abd72d561f92d29 | <!-- Authority: MESA_REMEDIATION_PLAN_FINAL_V2_1_2_2026-08-15.md, slice S4 (§15: "same migration identity + different checksum = FAIL... No silent overwrite"). GAP FOUND live while validating rows 74-75 in this same session (identical self-correction pattern to row 71->72 in S1): the row-74 trigger only fired BEFORE UPDATE OR DELETE, so a future plain INSERT re-registering an existing filename under a different checksum would be silently absorbed by ON CONFLICT DO NOTHING -- no bad data gets in, but no error either, violating "no silent overwrite". Empirically verified first, on an isolated scratch table (dropped immediately, zero residue anywhere real): a BEFORE INSERT ROW trigger fires and can RAISE even under ON CONFLICT DO NOTHING, since Postgres evaluates BEFORE ROW triggers ahead of conflict resolution. FIX: CREATE OR REPLACE the same trigger function adding a TG_OP='INSERT' branch (same-checksum re-insert stays a silent no-op; different-checksum re-insert now RAISEs 23505); DROP+CREATE the trigger (Postgres cannot ALTER a trigger's fired-event list) so it now fires BEFORE INSERT OR UPDATE OR DELETE. UPDATE/DELETE branches byte-identical to row 74. APPLIED ON STAGING (tdikhfeinufaahagmpjz) 2026-08-15 via Supabase MCP apply_migration; live-verified directly afterward against the real table (safe by construction -- every probe either fails closed with zero writes, or exactly matches already-recorded data): DELETE rejected, checksum-change UPDATE rejected, verified->unverified regression rejected, mismatched-checksum INSERT now raises 23505 (previously would have silently no-op'd), same-checksum INSERT stays a safe idempotent no-op, checksum-shape/verification_status/verified_chk CHECK constraints all reject malformed synthetic rows (never committed), anon/authenticated confirmed to hold zero grants with RLS enabled and zero policies. Lawful promotion path (bootstrapped_unverified -> verified) verified separately on an isolated scratch table, never against real rows. Paired `.ROLLBACK.sql` restores the pre-follow-up trigger (BEFORE UPDATE OR DELETE only) and function body verbatim. -->
| 77 | 2026-08-16 → **R-DAY1 — Business Day authority + atomic pointer substrate** | 2026-08-16_r_day1_business_day_authority_substrate.sql | (local, not yet committed) | 2026-08-16 | 5160063848cde6e5 | <!-- Authority: BUSINESS_DAY_R_DAY_IMPLEMENTATION_PLAN_V1_2026-08-16.md (R-DAY0, approved/frozen), §6-10/§15-17. First slice of the approved Strategy B (Business Day operational parent / Service Period economic child) pivot. Creates public.business_days (one row per business_date, no status column yet -- R-DAY5 adds status/sealed_at/seal_source/sealed_by together with the seal RPC that consumes them), public.business_day_policy (singleton POLICY row -- auto_seal_local_time/seal_timezone/ticket_reset_on_consolidation_default -- explicitly NOT the generic public.config key/value table, the authority-boundary fix R-DAY0 requires), and public.business_day_lifecycle_state (the ONE governed pointer row for {current_business_day_id, current_period_id, current_ticket_epoch}, guarded by business_day_lifecycle_state_guard_v1 so none of the three can be written independently of the sanctioned transition path -- BUSINESS_DAY_POINTER_UNAUTHORIZED_MUTATION raises otherwise). Backfills service_sessions.business_day_id deterministically (one business_days row per DISTINCT service_sessions.business_date, no inference) then SETs NOT NULL in the same transaction (safe here, unlike S6/S7's cross-deployment window, because no application code reads/writes the column yet). Adds public.open_business_day_v1 -- a DORMANT bootstrap RPC, not called by any committed application code in this slice, reusing the EXISTING 'service_session_lifecycle' advisory lock (deliberately not renamed). R-DAY1 does not create order_entities/order_uid (R-DAY2/S6), does not rewrite service_session_assign_order or change any order-intake decision (R-DAY3), does not implement period consolidation (R-DAY4) or Business Day SEAL (R-DAY5), and touches no row of public.config -- ORDER_RESET_TS/LAST_CLOSE_PRANZO/LAST_CLOSE_SERA/LAST_CLOSE_DATE remain untouched, classified LEGACY_COMPATIBILITY_ONLY, retirement deferred to R-DAY10 per R-DAY0's own retirement map. Live-verified on staging (tdikhfeinufaahagmpjz): 9 business_days rows bootstrapped from 9 distinct business_date values across 11 service_sessions (0 unmapped), payment_transactions population unchanged at 20 (13 historical + 4 TEST-S1 + 3 TEST-S2) before and after. 60/60 static structural tests (tests/rDay1BusinessDayAuthoritySubstrate.static.test.js). Paired `.ROLLBACK.sql` refuses if any R-DAY2+ object exists or if current_period_id has already been consumed for real; otherwise drops only R-DAY1-owned objects, never touches service_sessions.business_date/id/service_kind or any financial/order table. -->
| 78 | 2026-08-16 → **R-DAY2 — permanent order identity + ticket-epoch integration substrate** | 2026-08-16_r_day2_permanent_order_identity.sql | (local, not yet committed) | 2026-08-16 | 5f69305364aa402e | <!-- Authority: BUSINESS_DAY_R_DAY_IMPLEMENTATION_PLAN_V1_2026-08-16.md (R-DAY0, approved/frozen) §7-8, absorbing MESA_REMEDIATION_PLAN_FINAL_V2_1_2_2026-08-15.md slice S6 in its entirety (S6 retired as a standalone slice). Creates public.order_entities (append-only, permanent identity owner, amended per R-DAY0 §8.3 with NOT NULL business_day_id -- derived only through the order's own Service Period, service_sessions.business_day_id, NEVER through business_day_lifecycle_state, whose own live concurrency proof remains deferred to before its first live consumer per R-DAY1's certification -- and nullable ticket_epoch/ticket_number, real for every new row, NULL for historically backfilled rows with no honest value knowable). Adds public.ordenes.order_uid, NULLABLE (SET NOT NULL explicitly deferred to S7, respecting that slice's original ownership even though this design has no cross-deploy gap that would make it unsafe now). Adds public.order_entity_anchor_v1() + trigger ordenes_order_entity_anchor_v1 (BEFORE INSERT, sorting after ordenes_assign_service_session -- verified live: 'ordenes_order_entity_anchor_v1' > 'ordenes_assign_service_session' at the first differing character -- and always preceding the AFTER INSERT mesa_snapshot_order_lines_v1 by Postgres BEFORE/AFTER semantics, independent of naming) as the sole writer of order_entities for every new order across every channel (Mesa via table_session_id -> table_sessions.workspace_id; every other channel via the new mesa_singleton_workspace_v1() fail-closed tripwire, unchanged from the original frozen S6 §18.4 design). Deterministic historical backfill: every live ordenes row (already PK-unique) plus every storico row with NO live ordenes counterpart (explicitly disjoint by construction, avoiding any timestamp-based dedup fragility) -- 0 unresolvable rows (verified: 0 live ordenes/storico rows with NULL service_session_id, 0 with unresolvable creation timestamp). order_entities_ticket_uq UNIQUE(business_day_id, ticket_epoch, ticket_number) is safe NOW, not merely deferred, because the new ticket_number counter (business_days.next_ticket_number, established inert by R-DAY1, consumed here for the first time) shares no column, table or writer with the still-live legacy ordenes.id/#NNN mechanism in agentOrdini.js, which this migration leaves completely untouched -- zero collision surface. Does not perform S7's SET NOT NULL, does not touch S8's four append-only bypass tables, does not create S10's ledger_adjustment_operations/table_ledger_adjustments, does not read or write business_day_lifecycle_state's data anywhere (only its existence, via to_regclass, in the predecessor guard). Live-verified on staging (tdikhfeinufaahagmpjz): historical backfill covers ordenes ∪ storico deterministically with 0 unmapped; a real test order through the anchor produced exactly one order_entities row with a UUID order_uid unrelated to its display ticket, correct service_session_id/business_day_id/workspace_id lineage; retry via the existing ordenes_client_req_id_uniq idempotency backstop confirmed no duplicate anchor; UPDATE and DELETE against order_entities both rejected (MESA_APPEND_ONLY, reusing the existing generic trigger function); payment_transactions population unchanged at 20 before and after. 60/60 static structural tests (tests/rDay2PermanentOrderIdentity.static.test.js). Paired `.ROLLBACK.sql` refuses if any R-DAY4/S7/S10 object exists, or if any order_entities row shows created_at after this migration's own applied_at (real post-apply order intake -- destroying that evidence would be unsafe); otherwise drops only this slice's own objects. -->
| 79 | 2026-08-16 → **R-DAY2 HOTFIX — service_sessions.business_day_id auto-derivation** | 2026-08-16_r_day2_hotfix_service_sessions_business_day_derive.sql | (local, not yet committed) | 2026-08-16 | 41b48f6f9d41ba5d | <!-- Authority: discovered live during row 78's own verification, not part of R-DAY0's written spec. ROOT CAUSE: row 77 (R-DAY1) applied service_sessions.business_day_id SET NOT NULL with no default; neither public.ensure_service_session nor public.roll_service_session_economic_v1 (both pre-existing, unmodified by any R-DAY slice) ever supply that column in their own INSERT. Reproduced live in a rolled-back transaction before authoring this fix: INSERT INTO service_sessions(business_date,status,opened_by,open_source,service_kind) VALUES(...) -> 23502 null-value-in-column violates not-null constraint. BLAST RADIUS: inert while a session is currently open (both RPCs' own "already current" branches return before reaching their INSERT), live-breaking the next time either genuinely needs to open a new session -- a real, not hypothetical, regression on this staging database. FIX: a BEFORE INSERT trigger, service_sessions_business_day_derive_v1, deriving business_day_id from NEW.business_date via find-or-create against business_days (business_days_business_date_uq as the concurrency backstop, ON CONFLICT DO NOTHING + re-read), overwriting unconditionally so a forged value can never survive (matching S10's own child-derive pattern). Neither ensure_service_session nor roll_service_session_economic_v1 is modified -- both remain completely unaware this column exists. Does not touch business_day_lifecycle_state (same no-pointer-consumption discipline as row 78's own anchor writer). Post-condition reproduces the exact failing probe (using status='rolled_over' rather than 'open', to avoid colliding with the real open session under service_sessions_single_active_uq) and asserts it now succeeds, then fully deletes both the probe session row and its business_days side effect -- zero residue. Live-verified on staging (tdikhfeinufaahagmpjz): the original probe INSERT, retried after this fix, now succeeds and correctly derives business_day_id; re-verified ensure_service_session/roll_service_session_economic_v1 source untouched. 19/19 static structural tests (tests/rDay2HotfixServiceSessionsBusinessDayDerive.static.test.js). Paired `.ROLLBACK.sql` drops only the trigger and its function; never touches service_sessions data. -->
| 80 | 2026-08-16 → **R-DAY3 — order-intake authority flip: Service Period → Business Day** | 2026-08-16_r_day3_business_day_intake_authority.sql | (local, not yet committed) | 2026-08-16 | 69207e18ece4893b | <!-- Authority: BUSINESS_DAY_R_DAY_IMPLEMENTATION_PLAN_V1_2026-08-16.md (R-DAY0, frozen) + R_DAY3_INTAKE_AUTHORITY_AMENDMENT_V1_2026-08-16.md (frozen architecture amendment resolving a real contradiction R-DAY0 §17 left unaddressed: business_days.status does not exist until R-DAY5, and the true first live order-intake gate is src/serviceSessions/orderIntakePolicy.js -- called from agentOrdini.js BEFORE the INSERT -- not merely the service_session_assign_order() SQL trigger R-DAY0 §17 named). Introduces public.resolve_order_intake_context_v1() -- the ONE canonical, DB-authoritative intake-context resolver, called from the retargeted service_session_assign_order() trigger INSIDE the order INSERT transaction, under the existing service_session_lifecycle advisory lock. Restates serviceSchedule.js's DEFAULT_SCHEDULE constants in SQL (04:00/08:00/17:30/18:00), proven bit-exact via a 5760-minute-sample parity test spanning both DST transition weeks (tests/rDay3ScheduleParity.test.js). Removes Service-Period permission gating (NO_OPEN_SERVICE_SESSION/INVALID_OPEN_SERVICE_SESSION/STALE_SERVICE_SESSION) from both the SQL trigger and orderIntakePolicy.js (which additionally drops fetchActiveServiceSessionSelfHealing and the sessionRolloverClassification/incidentSafeRollover self-heal chain from the intake path entirely). C1: service_session_state.current_session_id becomes a mandatory write-through compatibility shadow, updated atomically in the same transaction as the canonical tuple -- required because get_current_service_closeout_session() (and everything built on getCurrentOperationalSession: getOrdenes, agentCucina's kitchen/delivery reads, scanServizio, backupSerata) raises SERVICE_SESSION_STATE_CORRUPT if the shadow ever points at a non-open/closing row. C2: service_sessions_date_kind_uq (historically globally-unique) replaced by service_sessions_date_kind_active_uq (unique only among open/closing) -- business_date+service_kind become classification, never identity; a period is never reopened. Two latent bare-EXISTS traps this exposes in ensure_service_session/roll_service_session_economic_v1 (both already retired from automatic intake, reachable only via manual/admin action) are re-scoped to active statuses, no other line touched. C3: getOperationalSessionIds() widened to also surface prior-day rolled_over sessions that still carry a non-terminal order or open table (bounded -- only rolled_over candidates, never a broad scan), so a Business Day advance can no longer make real unfinished work vanish from the board -- proven against the live fixture (yesterday's stale PRANZO period, 3 non-terminal orders, 1 open table) that motivated this whole slice. Rows 74/75/76 promoted bootstrapped_unverified->verified via S4's own established slug-match method before this migration's own predecessor guard will allow it to run. Paired `.ROLLBACK.sql` restores service_session_assign_order/ensure_service_session/roll_service_session_economic_v1 to their exact byte-captured predecessor bodies unconditionally (safe because of the C1 shadow), but REFUSES to restore service_sessions_date_kind_uq if any duplicate (business_date,service_kind) pair already exists -- the first R-DAY3 schema point of no return, whole-transaction abort, no destructive repair attempted. 55/55 static structural tests (tests/rDay3BusinessDayIntakeAuthority.static.test.js) + 6/6 schedule-parity tests. -->
| 81 | 2026-08-16 → **R-DAY3 HOTFIX — resolver bootstrap-demotion fix** | 2026-08-16_r_day3_hotfix_resolver_bootstrap_demotion.sql | (local, not yet committed) | 2026-08-16 | 14ac25b57ddd2e45 | <!-- Authority: discovered live during row 80's own pre-cutover acceptance testing, not part of R-DAY0/the amendment's written spec. ROOT CAUSE: resolve_order_intake_context_v1()'s demotion detection was keyed on business_day_lifecycle_state.current_period_id (the pointer), which R-DAY1 left dormant by design -- but staging's live legacy session (d20ee320, business_date=2026-08-15, status='open') predates the pointer entirely. On the resolver's first genuine call with the pointer still NULL, the demotion branch is skipped (nothing to demote, by the old logic's reasoning), so it instead tries to INSERT a new active period while d20ee320 is still open. Reproduced live, in a rolled-back transaction, before authoring this fix (an isolated pg_temp copy of the resolver's post-schedule-check logic, called with today's real (business_date,service_kind) against the real, unmodified staging state): INSERT INTO service_sessions(...) -> 23505 duplicate key value violates unique constraint "service_sessions_single_active_uq". BLAST RADIUS: would have fired on literally the next real order once the schedule window opened -- exactly the "stale previous-day Service Period blocks new-day intake" failure R-DAY3 exists to remove, just relocated to a raw constraint-violation crash instead of a typed rejection. FIX: demotion detection now queries what is ACTUALLY globally active in service_sessions directly (authoritative via service_sessions_single_active_uq, which guarantees at most one such row can ever exist) instead of trusting the pointer -- correctly unifying the steady-state case (pointer already correct), the stale-pointer case (already handled before), and the dormant-pointer-with-live-legacy-session case (the bug) into one code path. No other function/trigger/index from row 80 is touched. Post-condition is structural only (asserts the corrected/removed code patterns are present/absent in the live function body, and that d20ee320/the pointer/payment_transactions are untouched by this migration itself) -- deliberately never calls the real resolver inside this migration's own transaction, since doing so would perform an uncontrolled real activation dependent on the schedule window at apply time; the real live proof runs as its own separate, controlled, rollback-guarded acceptance-test step immediately after apply. 20/20 static structural tests (tests/rDay3HotfixResolverBootstrapDemotion.static.test.js). Paired `.ROLLBACK.sql` restores the exact byte-captured pre-hotfix (buggy) body -- only ever use together with row 80's own full rollback, never in isolation. -->
| 82 | 2026-08-16 → **R-DAY4 — Service Period consolidation: immutable economic checkpoint** | 2026-08-16_r_day4_period_consolidation.sql | (local, not yet committed) | 2026-08-16 | d16027cb408d97e3 | <!-- Authority: BUSINESS_DAY_R_DAY_IMPLEMENTATION_PLAN_V1_2026-08-16.md (R-DAY0, frozen) §9 exact DDL/RPC contract, §6 the one lock, §13 checkpoint-as-evidence, STOP condition 2 (consolidation must never gate/delay/reject intake), and the FINAL REPORT's explicit "the two [assignment and consolidation] are structurally incapable of being fused". OWNER CORRECTION (this session, before any code written): an earlier execution brief incorrectly assumed consolidating the CURRENT period must create/select a successor period and advance business_day_lifecycle_state.current_period_id/service_session_state.current_session_id -- contradicts §9 step 7 verbatim ("Nothing else. No table is touched. No order is touched. No payment is touched."). Corrected: consolidate_period_v1() NEVER touches the pointer, the legacy shadow, or service_sessions.status, on the current period or any other; a period may be consolidated while still current, and later orders may still attribute to it via the unchanged resolveEconomicPeriod(). Creates public.period_consolidations (append-only, R-DAY0's exact DDL, reuses the existing generic mesa_append_only_v1() trigger -- same discipline as order_entities) and public.consolidate_period_v1(p_workspace_id, p_period_id, p_by_actor, p_by_role, p_by_sid_hash, p_reset_tickets, p_client_request_id): acquires the EXISTING service_session_lifecycle lock (§6, shared with resolve_order_intake_context_v1) -> idempotency check on (workspace_id, client_request_id) -> server-assigned cutoff_at := clock_timestamp() (never a parameter) -> captures the checkpoint via the EXISTING, unchanged capture_closeout_snapshot() RPC (2026-08-08_service_closeout_incidents_foundation.sql), payload shape {session, orders, tableSessions, financialEvents} mirroring the one live reachable caller (economicBoundaryEngine.js) exactly -> inserts one period_consolidations row -> IF explicitly requested, advances business_days.ticket_epoch/resets next_ticket_number atomically in the same transaction, mirroring business_day_lifecycle_state.current_ticket_epoch only when the period's business day is the currently-pointed one. Workspace resolution fail-closed via the existing mesa_singleton_workspace_v1(). Monotonicity enforced procedurally inside the RPC (Postgres CHECK constraints cannot reference other rows) rather than as a DDL constraint; business_days.sealed_at half of §9's monotonicity clause not yet expressible (R-DAY5 not started). DETERMINISTIC PRIVILEGE FLOOR applied (same defect class as Slice 1.3): REVOKE ALL FROM service_role first, then grant back only SELECT, INSERT. Live-verified on staging (tdikhfeinufaahagmpjz) in a rolled-back smoke transaction against the real d20ee320 period before finalizing: ok:true/CONSOLIDATED, correct businessDayId/periodId/snapshotId, zero residue after rollback. Paired `.ROLLBACK.sql` refuses outright if any real consolidation checkpoint row exists -- a committed economic fact is never deleted to undo this slice; only the DDL/RPC objects are dropped, and only while the table is empty. -->
| 83 | 2026-08-16 → **R-DAY4 HOTFIX — pointer-authorization on ticket-epoch mirror write** | 2026-08-16_r_day4_hotfix_pointer_authorization.sql | (local, not yet committed) | 2026-08-16 | 0b2296cc4da41f77 | <!-- Authority: discovered live during row 82's own acceptance testing, not part of R-DAY0's written spec. ROOT CAUSE: business_day_lifecycle_state_guard_v1() (R-DAY1) rejects any write to current_business_day_id/current_period_id/current_ticket_epoch unless the transaction-local GUC ladieci.business_day_pointer_authorized='true' is set first -- resolve_order_intake_context_v1() (R-DAY3) already does this correctly; consolidate_period_v1()'s conditional ticket-epoch mirror write (p_reset_tickets=true, target period's business day is the currently-pointed one) omitted it. Reproduced live, for real, on staging during acceptance case C (consolidate the CURRENT period with p_reset_tickets=true): ERROR P0001 BUSINESS_DAY_POINTER_UNAUTHORIZED_MUTATION, whole transaction rolled back atomically, zero residue (period_consolidations row count, business_days.ticket_epoch, business_day_lifecycle_state.current_ticket_epoch all reconfirmed unchanged immediately after the failure). BLAST RADIUS: every reset_tickets=true consolidation of the CURRENT day's period, unconditionally; consolidating a non-current period or reset_tickets=false is unaffected (acceptance cases A/B/D, already proven live before this bug was found, untouched by this fix). FIX: adds the identical PERFORM set_config('ladieci.business_day_pointer_authorized','true',true) call immediately before the business_day_lifecycle_state UPDATE; no other line changes; still never touches current_business_day_id/current_period_id -- the owner-corrected non-fusion contract is unaffected, this only unblocks a write the function already intended to make. Re-verified live immediately after apply: acceptance case C replayed successfully (newTicketEpoch=2, resetTicketSequence=true, pointer/shadow/period-status otherwise unchanged), then replayed again under the SAME client_request_id and confirmed idempotent (no double-increment, epoch stayed 2, single period_consolidations row). 10/10 static structural tests (tests/rDay4HotfixPointerAuthorization.static.test.js). No runtime/JS change -- DB-only fix, RUNTIME_COMMIT stays e421bbd. Paired `.ROLLBACK.sql` restores the exact byte-captured pre-hotfix (buggy) body -- only ever use together with row 82's own full rollback, never in isolation. -->

> Rows 29–30: filename prefix `2026-07-24` is one day ahead of the `2026-07-23` commit date.
> `apply_order` places **workspace_foundation (S2-7B) before account_auth_boundary (S2-7C)**,
> overriding the lexical filename order which would (wrongly) invert them.

## Live-staging verification (owner / tooling action)

Confirming the *registered* version of each migration in the live staging database was **out
of scope for this session** (no Supabase Management/DB access was available here). When access
is present, verify for rows 29–30 that the applied name equals the source filename
(`2026-07-24_*`); if it matches, source and history are already consistent and no further
action is needed. Do **not** rename to "correct" the date after the fact — that would create
the very divergence this manifest prevents.


## S2-7D2 cutover ordering (rows 32–33)

The two S2-7D2 migrations are **not interchangeable** and must bracket the backend deploy:

1. **row 32** `2026-07-25_canonical_pin_rotation.sql` — adds `auth_set_actor_pin_v2`. Purely
   additive: the legacy writers still work, so an older backend keeps functioning.
2. **deploy the backend** that routes EVERY owner/operator/rider rotation through v2.
3. **verify** no call site references any of the six writers closed in step 4
   (`tests/pinRotationCutover.static.test.js`).
4. **row 33** `2026-07-26_disable_legacy_pin_rotation.sql` — fail-closes and revokes **all six**
   unsynchronized writers, each keeping its exact signature and `jsonb` return type:
   `auth_admin_set_actor_pin`, `auth_account_set_owner_pin`, `auth_set_pin_hash`,
   `auth_set_active`, `auth_admin_set_actor_active`, `auth_consume_recovery_window`.
   Its guard refuses to run unless row 32 is present, `service_role` can execute v2, and every
   one of the six is found with its exact expected overload.

> **Capability removed, not migrated.** Step 4 leaves **zero** writers of `auth_actors.active`
> and no operational recovery path. Neither is exposed by any runtime route today, so nothing
> regresses — but activation and emergency recovery stay disabled until they are redesigned
> around the canonical workspace lock in a later block. Operational and personal-account login,
> failed-attempt, unlock and session-revocation paths are untouched.

> **Transition boundary.** Between steps 1 and 4 the PIN-uniqueness invariant does **not**
> hold: any still-running old backend instance can call a legacy writer, which locks only
> (initiator, target) and performs no cross-actor duplicate check. Keep the window short and
> do not rotate operator/rider PINs inside it.

## S2-7D6E3 rider ledger rollout ordering (rows 36–37)

Same shape as the S2-7D2 cutover above — additive first, cleanup last, no step requires
simultaneity between Railway (backend) and Supabase (DB):

1. **row 36** `2026-07-27_s2_7d6e3a_rider_ledger_writer_additive.sql` — creates
   `_ledger_write_payment` and `rider_collect_and_complete_stop`, and replaces
   `order_mark_paid`'s body to delegate to the shared writer. `complete_rider_stop` is left
   untouched. Apply any time; the currently-deployed backend (which still calls
   `complete_rider_stop` and the pre-existing `order_mark_paid`) keeps working unmodified.
   This step alone also fixes an already-live gap: `order_mark_paid`'s pre-INSERT lookup
   queries had not been updated to match the session-scoped unique indexes
   `2026-07-26_two_service_identity.sql` installed, so an order id/number recycled across two
   service sessions could false-replay or spuriously conflict — see
   `tests/ledgerWritePaymentSessionScoping.test.js`.
2. **deploy the backend** commit that repoints `riderTrip.completeStop` from
   `complete_rider_stop` to `rider_collect_and_complete_stop`.
3. **smoke-test** the rider door-collection flow live: confirm `order_financial_events` rows
   appear with `by_role='rider'`, `meta.source='rider_delivery'`, and that a repeated
   "Entregado" tap on the SAME stop replays instead of double-charging.
4. **row 37** `2026-07-27_s2_7d6e3d_retire_complete_rider_stop.sql` — apply ONLY after step 3
   passes. Drops `complete_rider_stop(text, boolean, text)`. Its guard refuses to run unless
   row 36's `rider_collect_and_complete_stop` is present.

> **Transition boundary.** Between steps 1 and 4, `complete_rider_stop` still exists and would
> still work exactly as before if some caller reached it — nothing forces the cutover, so
> there is no unsafe window analogous to the PIN-rotation one above. The only requirement is
> ordering: never apply row 37 before step 2 has deployed and step 3 has passed.

## V3-J cutover ordering (row 51) — messa_* → mesa_*, MESSA_* → MESA_*

Unlike S2-7D2/S2-7D6E3 above, V3-J is **not additive-then-cleanup in two migrations** — it
is one atomic migration that creates every `mesa_*` function/trigger and drops every
`messa_*` one in the same transaction. What must still be staged in order is the
**application/DB pairing**, because the moment row 51 commits, the DB no longer has
`messa_*` at all, so the backend deployed at that instant must already call `mesa_*`.

1. **Apply row 50** (`2026-08-02_v3i_mesa_covers_deferred.sql`) to staging first — V3-J
   refuses to run without it (guard checks `messa_release_empty_session_v1` /
   `messa_guard_covers_monotonic_v1`).
2. **Deploy the backend commit that lands with this same slice** (mesaDao.js/mesaService.js/
   mesaHttpHandlers.js/supabaseResourcePolicy.js already call `mesa_*` and check `MESA_*`
   codes in this repo state) — but do **not** let it go live before step 3, since between
   step 1 and step 3 the DB still only has `messa_*`. If a real deploy window is needed
   between DB and backend, deploy the backend **after** row 51, not before.
3. **Update backend env** — no new/renamed backend env var for this slice (`MESA_HTTP_ENABLED`
   was already renamed by the S2-7D4/Mesa-staging predecessor slice; see the separate
   "ENV VAR CUTOVER" note in the session report, not this manifest).
4. **Apply row 51** (`2026-08-02_v3j_mesa_nomenclature_cutover.sql`) to staging.
5. **Update frontend env** — `REACT_APP_MESA_ENABLED` is already the live name (also renamed
   by the predecessor slice); nothing to change here for V3-J itself.
6. **Deploy the frontend** (uses `mesaApi.js`'s `MESA_*` dictionary already in this repo state).
7. **Smoke test**: open a Mesa, add a comanda, pay full/split/item/custom, create+seat+cancel
   a reservation, try to close the service with a Mesa still open (expect `MESA_TABLES_NOT_RELEASED`
   from the HTTP layer's `closeServiceOutcome.js` mapping, not the old `MESSA_` string).
8. **Legacy entry points are already gone** — row 51 drops every `messa_*` function inside the
   same transaction that creates `mesa_*`, so there is no separate "step 8" cleanup migration
   the way S2-7D2/S2-7D6E3 needed. Rollback (`2026-08-02_v3j_mesa_nomenclature_cutover.ROLLBACK.sql`)
   is the only way back to `messa_*`, and it must run before rolling the backend/frontend back.

> **Transition boundary.** Between steps 1 and 4 the backend and DB are inconsistent if the
> backend deploy in step 2 goes live before step 4's migration — a live backend calling
> `mesa_open_session_v1` against a DB that still only has `messa_open_session_v1` gets a clean
> `PGRST202`/function-not-found, not silent corruption, but it is a hard outage for the Mesa
> surface until row 51 lands. Keep this window as short as possible and apply row 51
> immediately before or immediately after the backend deploy, never hours apart.
